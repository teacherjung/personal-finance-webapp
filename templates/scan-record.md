# 掃描紀錄（本文 G1〜G3）——寫在變更說明的固定小標「### 複審後掃」下

**掃描時序**：對應的通過留言＝<獨立審查者第一次「通過」那一則的連結（審 `<版本碼>`）>；掃描時間＝<起〜訖>，在該則通過之後、轉正式之前；材料＝`<base 完整版本碼>`…`<head 完整版本碼>`。之後再出現新的通過不改指；每支只掃這一次。

**試探與比對**（掃前試探、掃後比對各印的最後一行，逐字；哪一支退非零就寫「未執行：<那一行>」，比對退 1＝事故）。⚠️ 埋假機密的清單檔**不要貼進來**，掃完之後 `--sweep` 一定要跑：

```
<逐字>
<逐字>
```

**配方聲明**（掃描腳本印出的那一行，逐字）：

```
<逐字>
```

**逐條判定**（按掃描器的編號）：

| # | 發現 | 判定 | 去處／觸發第幾輪複審 |
|---|---|---|---|
| 1 | <一句> | 屬實·已修 | 觸發 r<n> |
| 2 | <一句> | 屬實·進待辦 | <待辦的位置> |
| 3 | <一句> | 誤報 | — |
| 4 | <一句> | 待裁 | <❓ 留言連結> |

<details><summary>回覆原文（腳本寫下的檔，逐字）</summary>

```text
<逐字>
```

</details>

**掃完之後改了幾顆**：<版本碼>；不重掃。

沒掃成：同一小標下寫「未執行：<原因>」（本專案沒有隔離＝「未執行：無隔離」；試探不過＝「未執行：試探不過」並貼試探印的那幾行）；缺時序（沒有通過留言連結或時間）＝當未掃。**普通未執行記好後，不擋合併**，照本文其餘條件走；本專案沒有掃描器時，由 PROJECT-SETTINGS.md 指定的那一方寫未執行並轉正式。掃描不是正式放行。疑似破口＝事故，照本文 G2 走事故路徑，不套普通未執行的收口。

給掃描器的提示怎麼寫：只問機制、不要求它舉具體字串（原專案事故：要它給具體字串，它寫出憑證形狀，觸發自己的事故偵測、整遍判事故）。這條只適用於掃描器那條路，不是所有審查的規矩。
