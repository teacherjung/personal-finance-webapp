<!--
⚠️ 下面五個欄位是**必填**，由 scripts/check-pr-collab-fields.js 在合併前機械把關。
   規則見 AGENTS.md 的「三方協作框架」節。
   為什麼要機械把關：2026-08-02 實測連續三支（#374/#375/#376）漏填 —— 只靠記憶維持不住。
-->

## 協作欄位

- **實作者**：
- **獨立審查者**：
- **基準版本**：
- **預計修改的共享檔案**：
- **這支若完全失敗，最糟失去什麼**：

<!--
填寫說明：

- **實作者** / **獨立審查者**：Claude ／ Codex ／ William 之一。
  ⚠️ 兩者**不可以是同一個** —— 唯一不變量是「沒有任何一份產出，由寫它的人做正式複審與放行」。
  ⚠️ 按合併鍵的也不可以是實作者本人。

- **基準版本**：審查要釘住的 commit（短 SHA 即可）。
  分支被推過之後審查結論就失效了，所以審查者要能對得上。

- **預計修改的共享檔案**：AGENTS.md、公用模組、資料層…等會撞車的檔案。
  這一欄就是「共享檔案預約」——同時開著的 PR 靠它避免兩人同改一檔。
  真的沒有就寫「無」。

- **最糟失去什麼**：一句話。用來決定這支的流程重量（PR 分級見 AGENTS.md）。
-->

## 這一次實際改了什麼

## 怎麼驗收

<!-- 給 William 的三句白話：①開哪一頁 ②做什麼 ③看到什麼算對。不需驗收的支（只動考題／文件／腳本／CI 設定）就寫「不需驗收：只動了 …」——分級的正本＝REVIEW-AND-MERGE.md 合併步驟「回報合併結果與驗收分級」那一步。
     三關與突變測試的結果是給審查者看的，寫在上一節「這一次實際改了什麼」，不要放這裡。 -->

### Grok 複審後掃

_待補——時機與內容規格見 AGENTS.md「Grok 的邊界」節；實作者不是 Claude 就把這一節整個刪掉。這行還在＝還沒填。_

<!--
這一格**純指路，刻意不複述規格**（時機、要附哪幾樣、判定要用哪些值，全在 AGENTS「Grok 的邊界」節）——
文件複述一次就多一份會漂的副本，而這支 PR 修的正是那些副本各自漂掉的後果。
⚠️ 小標刻意用三個井號，跟本檔其他小標的階層不一致：AGENTS 釘死的字串就是 `### Grok 複審後掃`，
字串對齊優先於階層好看。
⚠️ 不擋合併。`test/grok-scan-docs.test.js` **會**機械檢查這個固定小標還在不在（那是約定的 grep key），
但**沒有任何機器檢查「待補」有沒有被填完、填得對不對**——那行只是讓「忘了填」跟「填過了」在事後 grep 時分得出來。
-->
