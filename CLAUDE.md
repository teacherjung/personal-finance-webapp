# CLAUDE.md

> 這份檔案**每個 session 都會自動載入**——是唯一保證被讀到的入口。所以它只放
> 「開工前非做不可、而且只有 Claude 需要知道」的事，**刻意保持極短**。
> 技術規則一律去 AGENTS.md，協作規矩一律去 RULES.md，不要在這裡長出第二份規則書（那會製造漂移副本）。

**技術的單一真相來源＝[AGENTS.md](AGENTS.md)**（架構、錢的絕對邊界、鐵則、投資語意、同步點；末節「本專案協作附則」＝只有本專案才有的協作值）。**協作規矩的正本＝[RULES.md](RULES.md)**（套件本文，Codex 與 Claude 共用；每條有哪台機器守＝[MACHINES.md](MACHINES.md)；機器登記與專案值＝`settings.json`，人讀的產物＝[PROJECT-SETTINGS.md](PROJECT-SETTINGS.md)）。

## 開工前

1. **先查 [docs/contracts/README.md](docs/contracts/README.md) 的路由表**——你要動的檔案落在哪個領域，就去讀那份契約。AGENTS.md 很大（大小刻意不寫死——寫死的數字自己會漂），「動手前先讀完」實際上做不到；路由表就是為此存在的。⚠️ AGENTS 的**全域段落**（錢的絕對邊界／鐵則）與 RULES.md 全文**不分任務一律適用、不經路由**。
2. **接手一段時間沒碰的工作**：先看 [PROJECT.md](PROJECT.md) 的「重要決定（已拍板，勿重議）」。
3. **要合併 PR**：在**釘住受審版本的樹的根目錄**跑 `node tools/merge.js <編號> --merger <識別值>`（RULES H1；它逐一跑 `settings.json` 登記為已啟用的閘、任一非零就停——閘的清單以 `settings.json` 的 gates 為準，**不憑印象**）；合併地點、退非零怎麼辦、合併後分級＝AGENTS「本專案協作附則」合併段。
4. **開工前也跑一次** `node tools/pending-rulings.js`（不收參數、掃整個專案的留言）——看有沒有「問了 William、他還沒回」的問題放在那裡（RULES D1、I2：**不是閘、不擋任何事**；問了就等他、沒有時限；留痕形狀＝`templates/ruling-record.md`，本專案的值在 AGENTS「本專案協作附則」的「審查回饋處置」那一顆）。

## 只有 Claude 需要知道的幾條

- **跟 William 的工作進度回報固定六段＋兩行**＝RULES J1、J2（六段與第七行「程序的附件」都在 J1，標籤與白話要求在那兩條，這裡不抄；第八行「進度摘要」是本專案自己的，見下一顆）。本專案的版面細節（他 2026-08-29 立、09-01 拍板全 session 適用、09-04 親自定稿）：六個標籤固定粗體、不加圖示、一律 J1 的順序、段間空一行；審查點與裁示問題各用一行清單（`- ①`；裁示問題的形狀＝RULES I1、I2）；「下一步在等什麼」寫在「達成了沒有」裡；還沒達成也不換標籤、內容第一句寫「還沒」；**問進度（「好了沒」「做到哪」）也走六段**，只有非進度的解釋、確認、選項才直接講；審查結果放**六段之後的程序附件**（六段裡的審查點只寫一句白話判定；⚠️ 附件那一行的每個網址**發出去之前先查一次**，不憑印象寫編號——2026-09-21 踩過，給了他一個編出來的留言編號）；多筆工作在同一套六段裡照主題歸類講「對他有什麼用」；其餘細節他問才講（他的原話：「我看不懂 or 你回報的內容太多了看不完」「盡量減少給我閱讀上的負擔」）。
- **第八行「進度摘要」**（他 2026-09-21 立，**只在本專案、不進套件**；同日稍早的「語音回報檔」已由他取消、由這一條取代）：規格、更新時機、檔案放哪、Codex 的責任——正本全在 AGENTS.md「本專案協作附則」的「回報」那一節，這裡不複述。
- **使用者的個人化脈絡**（投資原則沿革、決策紀錄）在 Claude 的 memory；**專案慣例一律寫進 AGENTS.md**，不要只寫在 memory——**Codex 看不到 memory**。
- **每一支 PR 的複審後掃**＝RULES G1〜G4（時機與不重掃都在那裡）：掃描發射者＝`settings.json` 的 scanner（現況 Claude）；紀錄照 `templates/scan-record.md` 寫在變更說明的 `### 複審後掃` 小標下。⚠️ **沒有任何合併閘看這件事**——漏掉不會有東西擋你，所以寫在這裡。
- **發現 Codex 的改動未 commit** 時：先審查再 commit，訊息標明出處（`Co-Authored-By: Codex`）。
- **⚠️ 不要在 worktree 裡刪除或重裝 `node_modules`**：那個 symlink 指回主目錄，動到它會讓使用者的 app 起不來（2026-08-02 實際踩過）。純閱讀分析不需要它；正式審查的 symlink 一律由發射者備樹時處理。
