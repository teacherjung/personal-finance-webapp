# CLAUDE.md

> 這份檔案**每個 session 都會自動載入**——是唯一保證被讀到的入口。所以它只放
> 「開工前非做不可、而且只有 Claude 需要知道」的事，**刻意保持極短**。
> 技術規則一律去 AGENTS.md，不要在這裡長出第二份規則書（那會製造漂移副本）。

**單一真相來源＝[AGENTS.md](AGENTS.md)**（架構、鐵則、投資語意、同步點、協作流程），Codex 與 Claude 共用，慣例衝突時以它為準。

**規矩住在哪＝[COLLAB-MAP.md](COLLAB-MAP.md)**（路由表：協作規矩分住`AGENTS.md`／`REVIEW-AND-MERGE.md`／本檔，哪一條在哪一節去那張表查。它**只指路、不是正本、無規則效力，而且刻意不完整**——衝突時以正本為準，表上查不到不代表沒那條規定）。

## 開工前

1. **先查 [docs/contracts/README.md](docs/contracts/README.md) 的路由表**——你要動的檔案落在哪個領域，就去讀那份契約。AGENTS.md 很大（大小刻意不寫死——寫死的數字自己會漂），「動手前先讀完」實際上做不到；路由表就是為此存在的。⚠️ AGENTS 的**全域段落**（錢的絕對邊界／鐵則／協作邊界）**不分任務一律適用、不經路由**。
2. **接手一段時間沒碰的工作**：先看 [PROJECT.md](PROJECT.md) 的「重要決定（已拍板，勿重議）」。
3. **要合併 PR**：照 [REVIEW-AND-MERGE.md](REVIEW-AND-MERGE.md) 的合併步驟走（步數同樣不寫死），**每一道機械閘都不可憑印象跳過**（現有幾道以腳本自報的 `MERGE_GATE` 為準——⚠️ **這裡刻意不寫數字**：2026-08-03 新增第三道之後這句話漏了它，而寫死的數字自己會漂）。

## 只有 Claude 需要知道的幾條

- **跟 William 的工作進度回報一律六段，標籤字眼固定**（他 2026-08-29 立、09-01 拍板全 session 適用、**09-04 親自定稿六項**；PR 說明等正式文件照各自規格寫、不套本條）：**目標**／**達成了沒有**／**有什麼改變**／**要你注意的**／**要你動手的**／**要你裁示的**——六個標籤逐字照抄、固定粗體、不加圖示、一律這個順序；段間空一行；審查點與裁示問題各用一行清單（`- ①`；**裁示問題每一點的內容形狀＝一句白話問題＋選項＋我建議的預設＋時限**，可分行但仍是同一點——選項上限、天數、哪些題有時限、哪些永遠等他（錢與金額口徑那類、以及正本列的其他幾類，沒有時限）、留痕怎麼貼，全在 AGENTS「審查回饋處置」節的「問法與逾時預設」那顆，本檔不複述）；空的段寫「沒有」、不省略；「下一步在等什麼」寫在「達成了沒有」裡；還沒達成也不換標籤、內容第一句寫「還沒」。**問進度（「好了沒」「做到哪」）也走六段**；只有非進度的解釋、確認、選項才直接講、不套六段——這條界線要穩：他分不出來就等於隨機（09-04 他抓到三套字混用）。一律白話、**我自己寫的字**不出現檔名／SHA／指令／術語（非講不可就當場翻成人話）——審查結果放**六段之後的程序附件**（附件的內容＝REVIEW-AND-MERGE「使用方式」的回報那條，本檔不複述；六段裡的審查點仍只寫一句白話判定；附件不併進六段的清單行、不另開第二套六段）；多筆工作**在同一套六段裡**照主題歸類講「對他有什麼用」（不另開第二套標籤）；其餘細節**他問才講**（他的原話：「我看不懂 or 你回報的內容太多了看不完」「盡量減少給我閱讀上的負擔」）。⚠️ **本條縮減的只有過程敘事**：錯誤、風險、需要他當下決定或**動手**的事（例：合併後要不要重啟、驗收要開哪一頁）照報不減；教學（當課講）與 AGENTS／REVIEW-AND-MERGE 要求主動回報的義務（含 REVIEW 開頭「回報」那條），不受本條影響。
- **使用者的個人化脈絡**（投資原則沿革、決策紀錄）在 Claude 的 memory；**專案慣例一律寫進 AGENTS.md**，不要只寫在 memory——**Codex 看不到 memory**。
- **Codex 現在也會實作**（William 明確指派時＝模式③）。他實作的支由**你複審、你按合併鍵**；反過來你實作的支不可以自己放行。唯一不變量：**沒有任何一份產出，由寫它的人做正式複審與放行**（完整說明在 AGENTS.md 三方協作框架節）。
- **你實作的 PR：Codex 說「通過」之後、`gh pr ready` 轉正式之前，要做一次 Grok 複審後掃**（做法見 [REVIEW-AND-MERGE.md](REVIEW-AND-MERGE.md)「怎麼執行」節那一條；規則正本＝AGENTS.md「Grok 的邊界」節）。⚠️ **每支只掃這一次**——掃到東西修完、Codex 再給一張「通過」時**不重掃**（重掃會把「第一張通過漏了什麼」這份證據蓋掉，那是本條唯一的產出）。⚠️ **沒有任何合併閘看這件事**——漏掉不會有東西擋你；而等你翻到合併步驟才第一次讀到它，PR 多半已經轉正式了（那時每修一條＝head 變＝重拿一張「通過」，整條複審鏈重走）。所以它寫在這裡，不是只寫在那兩份裡。
- **發現 Codex 的改動未 commit** 時：先審查再 commit，訊息標明出處（`Co-Authored-By: Codex`）。
- **⚠️ 不要在 worktree 裡刪除或重裝 `node_modules`**：那個 symlink 指回主目錄，動到它會讓使用者的 app 起不來（2026-08-02 實際踩過）。純閱讀分析不需要它；正式審查的 symlink 一律由發射者備樹時處理。
