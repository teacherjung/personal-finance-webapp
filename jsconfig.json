{
  "//": "零建置的 TypeScript 抓錯：不 build、不改副檔名。checkJs=false＝只檢查有 `// @ts-check` 的檔案（逐檔 opt-in）。編輯器(VS Code)會自動套用；CI/本機可跑 `npx -y --package typescript -- tsc -p jsconfig.json` 當守門。⚠️ test/ 刻意不在 include 裡（2026-09-11 William 裁；理由與量過的代價在 AGENTS「型別檢查」節）：考題檔檔頭寫的 `// @ts-check` 只有編輯器在看，`npm run typecheck` 不會開它們。test-doubles/ 與 prototype/ 從同一天起納入。",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "strict": false,
    "strictNullChecks": true,
    "skipLibCheck": true
  },
  "include": ["lib/**/*.js", "public/**/*.js", "public/**/*.d.ts", "scripts/**/*.js", "server.js", "test-doubles/**/*.js", "prototype/**/*.js"]
}
