// @ts-check
// 設定頁「AI 解析鑰匙」卡的**判準與文案**（P1b-2）。純函式、零 DOM——`settings.js` 頂層 import
// `app.js`（node 載不動整頁），判準留在那邊就只能靠形狀掃描；抽到這裡才有真正的行為考題。
//
// 三條硬前提：
// 1. **鑰匙絕不回顯**：前端只拿得到 `settings.aiApiKeySet` 布林（機密投影，同 flexToken）。
// 2. **留空＝不變更、勾清除＝送空字串**（同 flexToken／台新證券密碼的既有慣例，順序也一樣：清除優先）。
// 3. **文案在 LOCAL 與 HOSTED 都要成立**：卡片拿不到模式資訊，所以絕不可照抄台新那句「只存這台電腦、
//    永不上傳」——AI 鑰匙在 HOSTED 加密落庫，而且它本來就會以 x-api-key 送到供應商。
import { AI_COST_HINT } from './ai-consent.js';   // 費用級距只有一份住所（同意窗與設定頁共用同一句）

export const AI_KEY_CARD_TITLE = 'AI 解析鑰匙（Anthropic API key）';
export const AI_KEY_CARD_NOTE = '上傳的帳單如果系統看不懂版面（新的銀行、或銀行改版），可以在你按過確認之後交給 AI 讀一次——這把鑰匙就是付那一次費用的憑證。不設定也完全可以：現有認得的帳單照常匯入，一個字都不會變。這把鑰匙和 IB Token、帳單密碼一樣是被特別保護的欄位（存好之後不會再顯示出來，這個畫面只知道「有沒有設」）。AI 解析目前只在「裝在自己電腦上」的版本開放；網頁版（雲端）在隱私同意畫面做好之前一律停用，存了鑰匙也不會用到。';
export const AI_KEY_PLACEHOLDER_SET = '已設定，留空＝不變更';
export const AI_KEY_PLACEHOLDER_UNSET = '貼上 API key（sk-ant- 開頭）';
export const AI_KEY_CLEAR_LABEL = '清除已存的鑰匙（改回未設定）';
export const AI_KEY_SAVED_TEXT = 'AI 解析鑰匙已儲存';
export const AI_KEY_CLEARED_TEXT = '已清除 AI 解析鑰匙';
export const AI_KEY_NOCHANGE_TEXT = '沒有變更：貼上新的 API key，或勾選清除。';

/**
 * 設定頁那張卡要送出的 patch。**清除優先**（順序同 `saveTaishinSecPw` 的既有慣例）：
 * - 勾了清除 ⇒ `{ aiApiKey: '' }`（明確送空字串＝清空）
 * - 沒勾、有打字 ⇒ `{ aiApiKey: 值 }`。⚠️ **trim**：API key 從網頁複製極容易帶尾端換行，
 *   帶空白的鑰匙不會當場報錯，會等到下次上傳帳單才炸成 `ai_auth`（使用者以為鑰匙壞了）。
 * - 沒勾、也沒打（或只打了空白）⇒ `null`＝**沒有變更**（呼叫端紅字提示、不送 PUT）。
 *   ⚠️ 這一條就是「留空不會把已存的鑰匙清掉」的保證：寫成無條件送 `val()` 會讓使用者一按儲存就清空。
 * @param {{value?: any, clear?: any}} o @returns {{aiApiKey: string}|null}
 */
export function aiKeyPatch({ value, clear } = {}) {
  if (clear === true) return { aiApiKey: '' };
  const v = typeof value === 'string' ? value.trim() : '';
  return v ? { aiApiKey: v } : null;
}

/** 就地解釋（設定頁四顆 ⓘ）。全部是常數、零插值 ⇒ 交給 `openInfo` 不 esc 也安全。 */
export const AI_KEY_INFO = Object.freeze({
  what: {
    title: 'API key 是什麼？去哪辦？',
    html: `
<p>API key 是向 Anthropic（做 Claude 的那家公司）借用 AI 服務的「會員憑證」——一長串密碼。app 每次請 AI 讀帳單，就是拿這串憑證去敲門，對方憑它認人、記帳。</p>
<p><b>去哪辦：</b>到 <b>platform.claude.com</b> 註冊開發者帳號，進「API keys」建立一把，複製貼進上面的欄位。<b>那串字只會顯示一次</b>，關掉視窗就看不到了——沒存到就重新建一把，沒關係。</p>
<p><b>⚠️ 這跟你平常付月費用的 Claude 訂閱是兩套帳。</b>訂閱費不會折抵這裡的用量，這裡的用量也不會算進訂閱。要在同一個網站另外綁一次付款方式（可以先儲值、也可以設每月上限），兩邊各自出帳。</p>`,
  },
  cost: {
    title: '大概要花多少錢？',
    html: `
<p>AI 是按「字數」計費的，單位叫 token（一個中文字大約 1–2 個 token），送進去的字和吐回來的字分別算錢。</p>
<p>一份對帳單抽成純文字大概是幾千個 token，所以<b>解析一份的費用落在「幾塊台幣以內」的級距</b>——不是幾十、幾百。而且只有「系統看不懂版面」時才會送 AI：認得的帳單走既有規則，一毛錢都不花。</p>
<p><b>⚠️ 這是級距直覺，不是報價。</b>實際看帳單多長、有沒有因為第一次讀不準而換大一點的模型再讀一次；真正的數字以 Anthropic 那邊的帳單為準。想要硬上限，可以在 platform.claude.com 設每月用量上限，超過就自動停。</p>`,
  },
  where: {
    title: '鑰匙存在哪？帳單會送去哪？',
    html: `
<p><b>鑰匙</b>存在這個 app 自己的資料裡。一般的畫面與資料讀取<b>都不會把它送回瀏覽器</b>——這個畫面只知道「有沒有設」，不知道內容是什麼。存放方式跟其他機密欄位（IB Token、對帳單密碼）完全一樣：裝在自己電腦上的版本存在你硬碟的資料檔裡，雲端版會先加密再落庫。</p>
<p><b>⚠️ 唯一的例外是「匯出備份」</b>：裝在自己電腦上的版本，備份檔為了能完整還原<b>會含這把鑰匙</b>（跟 IB Token、帳單密碼一樣）——那個檔案請當成機密檔案保管，別隨手轉寄或丟雲端資料夾。雲端版的匯出則會把這些機密剝掉。</p>
<p><b>帳單</b>只有在你按下確認窗的「同意」那一次，才會把那份帳單抽出來的文字送到 Anthropic 的伺服器；沒同意就完全不送，一次同意只算那一次，不會被記住。</p>
<p>你確認過的那份解析結果會暫時放在伺服器的記憶體裡，讓你按下匯入時寫進去的<b>就是你剛剛看過的那一份</b>；程式重開就消失，不會存進資料庫、也不會寫進紀錄檔。原始的 PDF 檔本來就不會被留下來。</p>`,
  },
  skip: {
    title: '不設定會怎樣？',
    html: `
<p><b>完全可以不設定。</b>現在會動的匯入一個字都不會變——系統已經認得的帳單版面走的是既有規則，跟 AI 無關。</p>
<p>差別只有一個：遇到<b>系統看不懂的帳單</b>時，畫面都會先問你「要不要交給 AI 讀一次」——<b>有沒有設鑰匙都會問</b>（這個問句本身不會送出任何東西）。按下同意之後：在<b>裝在自己電腦上的版本</b>，沒設鑰匙會請你先來這裡設定、設了才真的送得出去；在<b>雲端版</b>，AI 這條路目前整條停用，不論有沒有設鑰匙都會直接告訴你還沒開放。兩種情況都一樣：每一次要送出前都得由你按過同意。</p>
<p>還有一件事要先知道：AI 讀出來的結果<b>還要通過餘額驗算才准匯入</b>——拿<b>台幣帳戶</b>的明細，上一筆餘額加減這一筆金額要剛好等於下一筆餘額；帳單上如果也印了那個帳戶的概要餘額，最後一筆會再跟它對一次。扣不起來就整份不收——寧可暫時不能匯入，也不收一份自己對不起來的帳單。</p>
<p><b>但它驗不到這些</b>：每個帳戶的<b>第一筆</b>（沒有前一筆可比）、<b>外幣</b>明細（本來就不計入台幣收支）、<b>這期沒有往來的帳戶</b>（只出現在概要、明細一筆都沒有——它的餘額仍會照帳單更新，但沒有明細可以驗它）、以及「金額和餘額一起抄錯成剛好自洽」的情況。所以設了鑰匙不代表什麼帳單都吃得下，匯入前也還是要自己看一眼。</p>`,
  },
});

/** 費用句在卡片說明區也要看得到一次（與同意窗同一份文字，改一處兩邊都跟著改）。 */
export const AI_KEY_COST_LINE = `費用：${AI_COST_HINT}。`;
