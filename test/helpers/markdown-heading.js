// @ts-check
// **判一行是不是「會讓後面的內容換一個爸爸」的標題**——給 `test/collab-invariant-docs.test.js`
// 的祖先標題鏈用。抽成獨立檔案是為了能用一張正反例表直接釘住它（見同名的 .test.js）。
//
// ## 只認 ATX（William 2026-09-07 裁，原話逐字「照你新講的做」，落點＝#578 裡他的留言）
//
// ⚠️ **他裁的只有「標題只認 ATX」這一件事**。下面「連隱藏區掃描都不做」是**Claude 自己的決定**，
// 不是他的話——理由是它只會讓考題更紅（不會更鬆），所以落在常設授權內，不必等他。
//
// 判準就是一行正規式：行首最多三格空白、一到六個井號、後面接空白／tab／行尾。**沒有別的規則。**
//
// ⚠️ **已知的假紅（刻意留著，不是漏掉）**：清單項底下縮排 1〜3 格的 `## …` 會被判成標題，
// 而它其實在清單容器裡、換不走本節的爸爸。要消掉它只有兩條路，兩條我都不能自己走：
//   ①改成「只認零縮排的 ATX」——那會讓「頂層縮排 1〜3 格的真標題」變成漏判，是**讓考題變鬆**，
//     屬「永遠等他」那一類；②重新實作清單容器解析——那正是 #579 連五輪證明不會收斂的路。
// 量過的風險：`git log -p --follow -- AGENTS.md` 整部歷史 1281 行新增裡，縮排 1〜3 格的 ATX **0 行**。
// 所以留著。真的踩到時它是紅的、訊息會直接說「這是已知的假紅」，改法一行（把那行改成零縮排或不用標題）。
//
// 為什麼連「圍欄／HTML 註解裡的不算」都不寫：拿 `git log -p --follow -- AGENTS.md` 量過**整部歷史**
// （1281 行曾被新增）——圍欄 **0** 行、HTML 註解 **0** 行、Setext 底線 **0** 行、四格以上縮排的 ATX **0** 行、
// 1〜3 格縮排的 ATX **0** 行；被新增過的標題 **23 行全部是零縮排的乾淨 ATX**。
// 也就是說那些例外規則在這份檔案上守到的量是 **0**，卻是 #578 那幾輪幾乎每一條 High 的來源：
// 它們一律屬於「看起來像隱藏區、其實不是」或「落單記號讓後面整片靜音」——**靜靜通過**型的失敗。
// 拿掉它們之後這支只會**更紅**（不再有任何東西能讓偵測靜音），不會更鬆。
//
// ⚠️ 誠實劃界（照實列，不是漏掉）：
// ・**Setext（下一行畫 `---`）與 raw HTML `<hN>` 標題**擋不到——William 的裁示內容，
//   由「⭐ 作廢字眼絆線」接住有寫「作廢／廢止」的那些。
// ・**容器裡的 ATX**（`- #### …`／`> #### …`／清單續行裡縮排 1〜3 格的 `## …`）：
//   前兩種偵測不到，第三種會被偵測到而**可能假紅**。兩個方向都可接受，因為容器裡的標題
//   只能重新分配那個容器內部的內容，而本節不在任何容器裡；而且這三種形狀歷史上 0 次。
// ・**萬一哪天 AGENTS.md 真的加了圍欄或 HTML 註解，而裡面剛好有一行長得像 ATX 標題**，
//   這支會假紅。那是**紅的、看得見、一行就能修**；相對地，為了它而養一個隱藏區掃描器，
//   換來的是「靜靜放過」的風險。這個取捨是刻意的。
//
// ⚠️ 這防的是「整理文件時手滑，把安全契約那一節收進一個新的小節」——用戶是未來的自己人，
// 不是攻擊者（沒有任何東西釘住那道考題本身要存在：實測把它整支刪掉，其餘相關考題全綠）。
// 手滑最可能產生的正是零縮排的 ATX，歷史上 23 次全都是。

/**
 * 第 i 行開啟的 ATX 標題層級；不是 ATX 標題回 0。
 * ⚠️ 井號後面一定要接空白、tab 或行尾——`#348（2026-08-02 合併）…` 這種「井號緊接數字」
 * 是 AGENTS.md 的真實內文（而且就坐在鏈最嚴格的那一段裡），少了這個條件就是當場假紅。
 * @param {string[]} arr @param {number} i
 */
export function headingAt(arr, i) {
  const m = /^ {0,3}(#{1,6})(?:[ \t]|$)/.exec(arr[i]);
  return m ? m[1].length : 0;
}
