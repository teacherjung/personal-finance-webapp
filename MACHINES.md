# 機器索引（本文每個「機器」標籤對到什麼）

本文（RULES.md）句尾的執行者標籤，定義在這裡、只在這裡（標籤只寫名字，不寫「機器：」前綴；**每個標籤都要以這張表上的某個名字開頭**，後面才接射程說明。`tests/rules-length.test.js` 會拿這張表對帳：表上沒有的名字、或名字沒擺在開頭，都會紅）：

| 標籤 | 意思 | 守不到的 |
|---|---|---|
| 雲端 | 合併前必過的雲端檢查，每次推送自動跑 | 要在平台上設成「必過檢查」且對管理員一樣生效，沒設＝退化成靠自覺 |
| 合併閘 | 合併步驟裡由合併者親手跑的腳本；跑了才擋 | 沒跑沒人擋；只認它讀得到的自報欄位與留言 |
| 鉤子 | 推送前在本機自動跑；每份複本要自己啟用 | 可被強制跳過；沒啟用的複本等於沒有 |
| 考題 | 測試卷裡的一題，改壞就紅 | 只守它斷言的那件事；不證明人有沒有遵守 |
| 攔截 | 工具被呼叫的當下被擋 | 只認清單上的工具名；每一方的攔截器要各自安裝、信任、啟用 |
| 掃描自檢 | 掃描腳本自己的前置檢查與事後驗屍；只有在被呼叫那一次會跑 | 沒人去跑就什麼都不會發生；綁著它跑的那台機器與那支掃描器 |
| 分級腳本 | 合併後算「這一支要驗到什麼程度」並印出來；只算不擋 | 有沒有照印出來的做完全靠自覺；路徑家族表沒列到的一律往重的算 |
| 清單腳本 | 被呼叫時把還欠著的事列出來（例如問了裁示者、還沒回的題）；只列不擋 | 有沒有人去跑、看了有沒有照做全靠自覺；只列得出留痕形狀對的那幾則 |
| 靠自覺 | 沒有機器 | 全部 |

## 專案設定

正本＝`settings.json`（值只有這一份）；`PROJECT-SETTINGS.md` 是由它產生的**產物**，給人讀、不要手改（改完跑 `node tools/build-settings.js`，考題比逐字元相同）。搬進專案後把每個「未設定」填掉：參與者識別值（值域與貼文帳號資格）、來源字串標準表、各清單與正本的位置（禁區、真實資料、忽略／豁免、路標範圍、主幹分支、三關命令、必要檢查、合併步驟登記、驗收依據與分級正本）、掃描發射者的登記方式、合併預授權，以及每支機器的啟用狀態。變更說明的「實作者」「獨立審查者」兩欄、結論標頭與自評的角色格，填的是識別值、不是職稱；「共享檔案」「最糟失去什麼」兩欄填內容。來源字串標準表是**寫作義務**：機器只驗來源非空、同一身分前後一致，不驗它在不在表上。

## 本文條目 → 機器

移植狀態（2026-09-13）：「移植狀態」欄寫「已就位」的在本倉庫並在本倉庫啟用；「已安裝未啟用」的程式與範本都在、等使用的專案填設定或接線；「原專案的實作」欄是 personal-finance-webapp main `7246efa` 的路徑，供搬運時定位。「啟用前提」驗不過的，那條標籤當靠自覺。

| 本文 | 原專案的實作 | 違反時會紅的條件 | 守不到的 | 啟用前提 | 移植狀態 |
|---|---|---|---|---|---|
| A2、E1 | 本倉庫 `tools/gates/check-collab-fields.js`＋`tests/check-collab-fields.test.js`＋`templates/pr-body.md`（平台的事全走 `tools/platform.js`；角色名單來自專案設定的參與者識別值） | 四欄任一缺（**只讀說明開頭那一段**：讀到第一個引用、程式碼圍欄、表格分隔列、HTML 與註解、圖片、連結定義、縮排或反引號配不完的行就停，訊息指出停在第幾行；所以圍欄、引用、註解裡的「範例欄位」不算填了，欄位要寫在最前面；讀法只有一份＝`tools/markdown-effective.js`）、角色看不出是剛好哪一位、實作者等於獨立審查者＝退 1；可用的識別值不到兩個、平台問不到、沒給編號＝退 2 | 欄位填的**內容是不是真的**（寫了某某不代表真的是某某複審的）；誰按的鍵；預約秩序；「共享檔案」「最糟失去什麼」兩欄只驗非空 | 專案設定填至少兩個純拉丁字母的參與者識別值；平台介面的 change 動作要登記好 | 已安裝未啟用（等專案填設定） |
| F4、F5 | 本倉庫 `tools/gates/check-review-verdicts.js`＋`tests/check-review-verdicts.test.js`＋`templates/verdict-header.md`（平台的事全走 `tools/platform.js`；角色名單來自參與者識別值） | 有未撤銷的阻擋、同一位同輪次相反結論、指定的獨立審查者沒對目前版本說通過、通過是對舊版本說的、讀不出指定誰（跟協作欄位閘同一個讀法：只讀說明開頭那一段）、**整份原文任何地方**有 🤖 記號但第一行標頭讀不出（未被作廢）＝退 1；可用識別值不到兩個、平台問不到、沒給編號、有留言的時間讀不出＝退 2。同一位同輪次的版本碼用**集合**聚合（取最長為代表、每一個都要是它的前綴，否則＝不明、擋；短碼相容不可傳遞，任何排列判一樣）；作廢的目標要確實比作廢者早，同一刻不生效；留言時間要是帶時區、日子真的存在的時間（契約同一份）；找 🤖 讀整份原文、什麼都不剝（引用、圍欄、行內程式碼、註解裡的都算：每一種剝法都在正常寫法上吞過看得見的 🤖；範本本來就寫明非結論留言不可含 🤖）；「像不像結論」的提醒只讀開頭那一段；標頭讀原文第一個非空行、作廢行讀標頭後緊接的收件區 | 標頭是自我宣告不是身分證明（改來源就能冒充另一位，要靠獨立帳號）；留言清單有沒有少給；沒帶記號的疑似結論只提醒不擋；**作廢比原專案寬**：任何一位審查者都能作廢任何壞標頭留言、不需裁示者特准（原專案的重述與豁免兩套約四百行，裁示者 2026-09-13 裁「甲」＝收成一行「作廢上一則：編號」；作廢不產生結論、作廢不了合規留言、作廢不了比自己晚的） | 專案設定填參與者識別值；平台介面的 change 與 comments 動作登記好 | 已安裝未啟用（等專案填設定） |
| B1 | 本倉庫 `tools/forbidden-tools.js`＋`tests/forbidden-tools.test.js`＋`templates/hook-claude.json`、`templates/hook-codex.json`（Codex 專案層）、`templates/hook-codex-global.json`（Codex 全域層，二擇一）（判斷一份、三份接線都用絕對位置呼叫它、起不來一律退 2；禁區清單住 `settings.json` 的 forbidden）；全域層讀 `tools/guard-copy.js` 從已提交版本抽出的固定複本，指令裡寫死指紋＝`tests/guard-copy.test.js`；放進宣告 type:module 的專案照樣起得來＝`tests/placement.test.js` | 工具名不合法、清單沒填或沒有任何一條有效規則、清單欄位型別錯或有一項不合文法（空白、非法字元＝不可能匹配合法工具名）、動禁區的連接器（含登記名帶 __ 的、在名字任何一段的）上不在唯讀名單（在名單上也照樣走家族網）、逐字在拒絕清單、命中家族網（點、連字號、連續底線都當同一個分隔）＝當場拒絕；Codex 全域層的複本不在、少檔、被改一個位元組、佔位沒換、複本裡有任何連結或查不了有沒有連結＝退 2（全擋），node 帶 --preserve-symlinks 兩旗標、照複本裡的位置讀旁邊的檔，攔截器說拒絕時全域層也退 2（不靠標準輸出）；抽複本的工具只接受已在 origin/<主幹> 裡的版本、範本也從那個版本讀，目的地在倉庫、暫存區、家目錄 .codex／.claude 底下（大小寫不同、目錄還不存在也算）就拒絕，範本那一組形狀不對（matcher 不是 ^mcp__、不是剛好一條指令）就拒絕，只靠樣式規則的清單沒給 --deny-probe 就拒絕，試跑三次（放行、擋、改過的副本）任一次不符就不印接線 | 只認工具名不看參數；詞表沒列到的擋不住；每一方要各自安裝、信任、啟用（沒啟用＝沒有）；接線範本只用 sh 模擬過、沒在真的 AI session 掛上去驗過；鉤子逾時不擋；全域層照抽的那個版本判：settings.json 的 forbidden、tools/forbidden-tools.js、tools/settings-data.js、tools/package.json 或 templates/hook-codex-global.json 那一組合併了改動而沒重抽＝照舊的判（沒有機器提醒）；驗收步驟靠人照 tools/guard-copy.js 檔頭做，沒有機器確認真的叫了無害測試工具、也沒有機器擋人拿真工具來試；指紋不蓋 node 與 shasum 本身、也不蓋登入設定決定的 PATH 找到哪一支 node；試跑是在工具自己的環境裡跑，Codex 起鉤子的環境不一定一樣；「已合併」看本機的 origin 追蹤參照；需要 /bin/sh、/usr/bin/shasum、/usr/bin/find（Windows 不支援）；Codex 專案層那份拒絕仍靠標準輸出 | 專案設定填禁區清單；兩家鉤子各自裝好按信任（Codex 全域層：跑 `tools/guard-copy.js` 抽複本、貼它印出的那一組） | 已安裝未啟用（等專案填清單並啟用鉤子） |
| B4、E7 | 本倉庫 `tests/ignore-lists.test.js`（登記在 `settings.json` 的 ignoreLists；本倉庫自己的 .gitignore 也登記在內） | 登記的忽略檔生效樣式跟登記的不一字不差就紅 | 清單合不合理；沒登記的忽略機制 | 專案把自己的忽略檔登記進去 | 已就位 |
| E2、E3 | 本倉庫 `tools/run-checks.js`＋`tests/run-checks.test.js`＋`templates/pre-push`、`templates/ci-github.yml`、`templates/collab-fields-github.yml`（三關命令住 `settings.json` 的 checks，鉤子與雲端都呼叫執行器、不抄） | 三關沒登記、或登記裡混一條未設定／空的／形狀錯（有一關還沒接好）＝退 2、一條都不跑；任一關紅＝退 1；跑之前不是工作樹根目錄（裸倉庫、樹被指到別處、在子目錄跑）、有提交卻這棵樹的索引檔不見或讀不了＝退 2（空索引、Git 支援的裸儲存庫形狀都是正常）；跑完才變成這幾種之一、**或三關前後倉庫的共用設定／這棵樹自己的設定內容不同**（比對設定檔位元組；就算從這棵樹看生效值健康）＝退 1；起不來＝退 2 | 鉤子被跳過或沒啟用；雲端沒設成必過檢查；三關命令本身對不對；索引檔在但只壞一部分；別棵樹自己的 config.worktree 被寫壞（只有在那棵樹裡跑才看得到）；三關開始之前就已經壞掉、但這棵樹生效值健康的共用設定；include 引進的其他設定檔被改；中途改了又還原；設定以外的倉庫損壞（不是 fsck）；同一段時間別的工作階段改了倉庫設定（會紅）；會改 Git 設定的合法準備步放進三關裡第一次會紅（要在三關外先做完） | 每份複本啟用鉤子；雲端範本放進去並設必過檢查 | 已安裝未啟用（範本在，各專案要接） |
| E4 | 本倉庫 `tools/git-env.js`＋`tests/git-env.test.js`；呼叫點＝`tools/platform.js`（所有閘問平台都經它）、`tools/merge.js`、`tools/run-checks.js`、`tools/gates/check-cross-merge.js`——四處各有一題釘子行程看不到 GIT_DIR | 共用實作退化（GIT_ 前綴沒清乾淨、GITHUB_ 被誤殺、原本那份被改）就紅；已知呼叫點的子行程看得到 GIT_DIR 就紅 | 新呼叫點不會被自動找到（每支新的閘或工具各補一題） | 無 | 已就位 |
| E5 | 每一道閘自己的考題（各有「真的跑一遍指令」與「問不到＝退 2」兩題） | 閘的指令入口不傳退出碼、平台問不到改退 0、吞例外，就紅 | 未來新閘要自己補這兩題（考題檔清單 `tests/manifest.json` 與金絲雀只抓「考題檔少了」，抓不到「新閘沒補這兩題」） | 無 | 已就位 |
| E6 | 本倉庫 `tests/file-names.test.js` | 已追蹤路徑含非可見 ASCII 就紅；例外名單寫死在考題裡、只出不進（本倉庫為空） | 未追蹤的檔；名字對不對得起內容 | 無 | 已就位 |
| G2 | 本倉庫 `tools/scan-probe.js`（掃前試探＋埋／收掃描期間的假機密）＋`tools/scan-postmortem.js`（掃後比對）＋`tests/scan-probe.test.js`、`tests/scan-postmortem.test.js`。隔離本身**不在套件裡**：由 `settings.json` 的隔離提供者、指令前綴（`{box}`＝盒子、`{projectRoot}`＝專案根目錄）、盒子位置、禁區清單指定（裁示者 2026-09-12 裁「丙」）。原專案的 `scripts/grok-scan.js` 家族只供參考 | 試探：沒有提供者、前綴或禁區清單沒填、假機密埋不進去、盒外對照讀不到、**盒內連自己的檔都讀不到或寫不進**、探針測不出＝退 2 不掃；任一禁區從盒內讀得到或寫得進（看檔案有沒有真的落地，不只看退出碼）＝退 1 不掃。比對：假機密或未給盒子的真值（原文、JSON 轉義一層、兩層）出現在回覆的內容或**檔名**、日誌裡每個檔與**目錄**的名字或內容、**日誌根自己的名字**、日誌根或子項是符號連結、**解不成 UTF-8 的內容逐位元組比到**＝退 1 事故（命中優先於驗不了）；沒有機密可比、機密太短、沒有輸出（空根又沒回覆也算）、有輸出驗不了（讀不到、讀不進、超過上限、解不成 UTF-8 而位元組也比不到）＝退 2 | **只測它自己用的那兩個機制**（讀＝cat、寫＝shell 重導向）：盒內擋掉這一支用的程式、但掃描器自己（例如 node）寫得出去的隔離，它測不到——會照實說測不出，不會說擋住（2026-09-13 稽核用真沙盒實測到的形狀）。隔離的其他面（網路、剪貼簿、環境變數、沒登記的目錄）不試；編碼過、拆段過、轉義超過兩層的外洩認不得；憑形狀認機密沒搬（原專案那一族是假事故最多的地方）；轉送器、足跡計數、掃描器版本鎖沒搬 | 專案設定填隔離提供者、前綴、盒子位置、禁區清單；在那個專案真的跑一次試探 | 已就位 |
| H1 | 本倉庫 `tests/settings.test.js` 的登記對帳題（磁碟 `tools/gates/` vs `settings.json` 的 gates） | 磁碟上有閘沒登記、登記了磁碟上沒有或路徑不在 tools/gates/、同一支登記兩次、登記形狀變了，就紅（每一筆先驗再對帳，不濾） | 閘有沒有效（各閘自己的考題）；有沒有人跑（合併指令） | 無 | 已就位 |
| H2 | 本倉庫 `tools/gates/check-stacked.js`＋`tests/check-stacked.test.js`（平台的事全走 `tools/platform.js`） | 底不是設定裡的主幹、有別支以這一支的頭為底＝退 1；來自別的倉庫、平台問不到、主幹名沒填、沒給編號＝退 2 不放行 | 有沒有人去跑它；平台回的答案是不是真的；**開著的變更清單有沒有少給**（分頁是那條登記指令自己的責任，這裡驗不到，少給＝漏擋） | 平台介面的 change 與 openChanges 兩個動作要登記好 | 已安裝未啟用（等專案填平台介面） |
| H3 | 本倉庫 `tools/gates/check-checks-really-ran.js`＋`tests/check-checks-really-ran.test.js`（平台的事全走 `tools/platform.js`） | 平台的自動合併開著、最新那一刻的場次結論不是成功（跳過也算沒跑）、還有場次在跑＝退 1；完成了卻沒有合法**帶時區**的完成時間或結論＝退 2（時間契約只有一份＝`tools/time-point.js`：日子要真的存在、時分秒與時區在值域內，不靠轉換器進位；時間用時間值比、毫秒以下算同刻；同一份資料在任何執行時區判一樣）；必過名單是空的、找不到正牌場次、平台問不到、主幹名沒填、沒給編號＝退 2 不放行 | 有沒有人去跑它；平台回的答案是不是真的；**場次清單有沒有少給**（分頁是那條登記指令的責任，少給會讓最新那一場被漏掉——這個方向是危險的，照實寫在這裡） | 平台介面的 change、requiredChecks、checks 三個動作要登記好 | 已安裝未啟用（等專案填平台介面） |
| H4 | 本倉庫 `tools/gates/check-cross-merge.js`＋`tests/check-cross-merge.test.js`（平台的事全走 `tools/platform.js`；三關與臨時樹準備指令登記在 `settings.json` 的 checks） | 以主幹為底的每支開著變更試合併後：文字衝突、三關紅＝退 1；試合併失敗卻沒有未合併檔案、三關起不來、被訊號殺掉、126／127＝退 2（不推定成因）；三關沒登記或登記裡混一條壞的、主幹名沒填、不在版本控制樹裡、本機沒有那些版本、平台問不到＝退 2 | 兩支合起來語意矛盾但三關沒覆蓋到；主幹在試合併後又前進；**臨時樹的執行環境跟合併後的相依清單一不一致**——那是準備指令與三關指令自己的責任（原專案為此寫了二百五十行套件管理器專用的核對，沒搬；準備指令建議在臨時樹裡重新安裝——「不會拿舊套件跑出假結果」是**有正確準備指令時**才成立的，不是一般保證）；重跑次數不計；開著的變更清單有沒有少給 | 專案設定填三關指令（與需要的話臨時樹準備指令）；平台介面的 change 與 openChanges 登記好；本機要有那些版本 | 已安裝未啟用（等專案填設定） |
| H6 | 本倉庫 `tools/acceptance-tier.js`＋`tests/acceptance-tier.test.js`（路徑家族表住 `settings.json` 的 acceptance） | 只算不擋：動作累積、級別寫最重；沒列到的路徑當指定那一級並列出；改名或複製沒帶舊路徑、平台自報總數跟清單對不上（清單被端點筆數上限截掉）＝退 2；表沒填好、平台問不到、沒有路徑＝退 2 | 表對不對；有沒有照做；平台沒自報總數時清單完整性沒對帳（會在輸出裡註明） | 專案填分級表；平台介面的 changedFiles 登記好 | 已安裝未啟用（等專案填表） |
| E5、H1 | 本倉庫 `tools/platform.js`＋`tests/platform.test.js`（登記在 `settings.json` 的 platform.operations） | 被問到的動作沒登記、樣板用了 {project} 而設定沒填、指令起不來或被殺掉、退出碼非零、印出來不是 JSON、答案形狀不合（少欄位、型別不對、單行欄位有換行或空字串、多出沒約定的欄位——連原型鏈上的名字也算多出來）、指令樣板沒用到它該用的參數，全部丟錯讓閘退 2；**絕不回空答案** | **答案是不是真的**——這一層只驗形狀，翻譯寫錯或平台騙人都擋不住（原專案那些閘同樣只能相信平台回的東西）；也不驗那條指令有沒有副作用；問到**哪個倉庫**由翻譯負責釘住（隨附範本用 {project} 釘、並登記要清掉的選倉環境變數）——翻譯沒釘就會被環境變數改寫 | 專案把每個動作（現在十個）各填一條指令、填 platform.project 與 clearEnv，指令要印出約定形狀的 JSON | 已就位 |
| H1 | 本倉庫 `tools/merge.js`＋`tests/merge.test.js`（閘的登記在 `settings.json`） | 沒有任何一道閘登記成「已啟用」、主幹分支名沒填、沒給變更編號、任一道閘非零、合併指令沒登記，都不放行；任一道紅就停在那裡，後面的閘與合併鍵都不動（閘退 1＝擋下、退 2 或其他非零＝查不清楚；node 閘的檔案不在或讀不到＝查不清楚，不跑）；開跑前問不到版本、按鍵前版本變了或問不到＝不按；合併指令的記號認不得、{merger} 沒報或不是登記過的參與者、{reviewer} 讀不出、{project} 沒填＝不放行 | 擋不住「不用這支指令、直接按平台的合併鍵」（那要平台級的分支保護）；也不驗那幾道閘自己對不對；**不讀合併預授權（A5）**——待裁未結、裁示者叫停、前置驗收未完成這三件它一概不知道，那條仍是靠自覺；{merger} 是自報的（驗不到真的是他，所以 A4、H5 仍標靠自覺）；版本只比頭尾兩次；node 閘檔在、但載入就崩（語法錯、相依缺）仍退 1、分不出來 | 無 | 已就位 |
| E1、G3 | 本倉庫 `tests/settings.test.js` 的範本那一題 | 機器索引指到的範本檔不存在、倉庫裡的範本沒被索引指到、變更說明範本吐不出固定小標「### 複審後掃」、掃描紀錄範本吐不出「掃描時序」（要在範本開頭那一段、某一行的行首：第一個特殊行之後的不算），就紅 | 有沒有照範本寫（那是靠自覺）；範本內容對不對；其餘小標字串沒逐一釘 | 無 | 已就位 |
| K1 | 本倉庫 `tools/build-settings.js`＋`tests/settings.test.js`（專案設定的值存 `settings.json`，說明是產物） | PROJECT-SETTINGS.md 與重新產生的結果不一致、欄位不合契約、啟用狀態不是約定的三種、機器標了不存在的條號，就紅 | 欄位填的值對不對、「已啟用」是不是真的啟用 | 無 | 已就位 |
| K2 | 本倉庫 `tests/rules-length.test.js`＋`tools/build-rules.js`（規矩存 `rules.json`，本文是產物；資料契約＋所有 ASCII 標點一律跳脫） | 字數超過、資料不合契約（非單行／空／前後帶空白）、標籤沒以已定義名字開頭、編碼漏跳脫或解回來與原文不同、RULES.md 與產生結果不一致，就紅 | 作者把語意上的新規矩寫成一個仍然合格的標題或射程說明；「解回來與原文相同」只保證本地解碼（呈現器對空字元另有替換，已由契約擋掉，其餘呈現差異不在保證內）；也不宣稱任何未來改法都繞不過 | 無 | 已就位 |
| 案例簿 | 本倉庫 `tests/casebook.test.js`＋`tools/build-casebook-index.js`（索引由案例檔產生、是產物；產生器的輸入與本文共用同一套契約與字面編碼） | 案例或索引含 CR 之類的換行或 HTML 註解記號、案例檔不是固定形狀（標題、空行、六欄依序、最多一行「相關」）、**索引與重新產生的結果不逐字元相同**、索引產生器的輸入不合契約（日期、檔名、條號的固定寫法，散文單行非空）、條號不存在、本文有條規矩沒案例指著、案例或索引出現字表上的名字（不分大小寫、整行照掃），就紅；帶不帶案例簿只看設定機器表這一列的登記（已啟用＝帶、未移植＝不帶），這一列不是剛好一列、登記成別的狀態、登記帶卻沒有 cases/、登記不帶卻有 cases/，也紅（照跑檢查）；唯一跳過＝登記不帶而且 cases/ 確實不在，四題顯示跳過並寫原因（考題數著跑了幾題，跳過接錯就紅） | 同一個變更裡同時把登記改成未移植又刪掉 cases/（兩樣都改就全綠，看得見的改動、靠審查）；不帶案例簿的專案裡「本文每一條都有案例指著」與名字字表不檢查（規矩本文要在套件那邊改）；「在不在」看磁碟、不看版本控制；欄位「非空」指的是原始欄值非空、不是呈現出來看得到字（這一題不解讀標記語言，只擋掉已知會產生隱藏內容的那兩樣）；事實對不對、一事一檔、來源連結活不活著、條號撐不撐得住那條規矩（字面引用≠事件支撐）、字表以外的新產品名與變形拼寫、其他個人狀況 | 帶案例簿＝設定這一列已啟用；不帶＝登記未移植（這台不跑） | 已就位（本倉庫帶；不帶的專案登記未移植） |
| K3 | 本倉庫 `tests/signposts.test.js` | tests/ 第一層與 helpers/ 第一層，帶記號的引號路標指不到、或壞掉，就紅 | 不帶記號的指路；跨檔；指錯地方；更深的子目錄 | 無 | 已就位 |
| D1 | 本倉庫 `tools/pending-rulings.js`＋`tests/pending-rulings.test.js`＋`templates/ruling-record.md`（形狀正本） | 只列不擋：三種留痕只認範本形狀、只採計登記的貼文帳號（外人的問不入、外人的撤回關不了題）；裁示要由裁示者的帳號貼且有原話行（轉述者是登記的識別值、不帶記號）；撤回要三選一理由與自報句；第一行讀原文第一個非空行（留痕標題不在第一行＝疑似）；原話、撤回理由、自報句、配對只讀**開頭那一段**（讀到第一個引用、程式碼圍欄、表格分隔列、HTML 與註解、圖片、連結定義、縮排或反引號配不完的行就停，疑似的原因附停在第幾行；讀法只有一份＝`tools/markdown-effective.js`；所以這些要緊接在標題下、寫在引用與程式碼之前）；配對要引到原問的編號、比問晚（時間契約同一份、要帶時區、日子要真的存在）；掃整個專案的留言（allComments）：題目所屬的變更關了照樣列、裁示貼在別支照樣配；設定沒填、平台問不到、有留言時間讀不出或沒帶時區、指令列給了任何參數＝退 2 | 整個專案的留言有沒有給齊（分頁是登記指令的責任）；已裁是推導；原話只驗形狀；不擋任何事 | 專案填裁示者識別值與帳號；平台介面登記好 | 已安裝未啟用（等專案填設定） |

## 範本（本文指到「範本」的地方）

- `templates/pr-body.md`：變更說明四欄與固定小標（E1、G3）。
- `templates/verdict-header.md`：結論留言機器標頭、三種留言的分法、撤銷條件（F4、F5、F7）。
- `templates/review-fixed-dimensions.md`：審查固定維度＋條件列（F2）。
- `templates/self-review.md`：公開自評的首行、四格與邊界（F9）。
- `templates/third-round-self-check.md`：第三輪自問表五題（F10）。
- `templates/scan-record.md`：掃描紀錄與未執行收口（G3）。
- `templates/ruling-record.md`：問裁示者、裁示落地、合規撤回的留痕形狀（I1、I3）。
- `templates/platform-github.json`：平台介面每個動作（現在十個）的翻譯範例（E5、H1），外加合併指令的範例。**不是正本**，是抄過去填進 `settings.json` 的起點；2026-09-13 對真倉庫實跑過。
- `templates/hook-claude.json`、`templates/hook-codex.json`、`templates/hook-codex-global.json`：禁區攔截器兩家 AI 的接線範例（B1；Codex 專案層與全域層二擇一），都只呼叫 `tools/forbidden-tools.js`，路徑不靠當下目錄、起不來一律退 2。全域層那份有三個佔位（複本路徑、檔案清單、指紋），由 `tools/guard-copy.js` 填，不要手填。
- `templates/pre-push`：推送前鉤子（E2），清 GIT_ 之後把三關交給 `tools/run-checks.js`。
- `templates/ci-github.yml`、`templates/collab-fields-github.yml`：雲端三關與協作欄位閘的雲端版（E3、E1）。
