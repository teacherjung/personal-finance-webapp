# 個人實際資料與憑證（含 IBKR flexToken、帳戶餘額）——絕不進版控
data/store.json
data/store.json.bak
data/store.db
data/store.db.bak
data/*.db
data/*.db-wal
data/*.db-shm
data/store.manual-backup-*.json
data/*backup*
*.bak

# Claude Code 本機設定（各機不同）
.claude/settings.local.json

# 相依套件
# ⚠️ 不可寫成 `node_modules/`（帶斜線＝只比對「目錄」）：worktree 會把 node_modules 做成
# **symlink** 指回主目錄，而 symlink 對 Git 來說不是目錄 → 不帶斜線才擋得住
#（2026-07-19 實際踩到：symlink 被 git add -A 收進 commit，還把本機絕對路徑一起帶上去）
node_modules

# 產生的檔案
# 審查循環的產出（scripts/review-loop.js）——每次跑都會長，不進版控
.codex-reviews/
tmp/
output/
*.log

# 系統
.DS_Store
**/.DS_Store
