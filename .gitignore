# 個人實際資料與憑證（含 IBKR flexToken、帳戶餘額）——絕不進版控
data/store.json
data/store.json.bak
data/store.db
data/store.db.bak
data/*.db
data/*.db-wal
data/*.db-shm
data/store.manual-backup-*.json
data/*backup*
*.bak

# Claude Code 本機設定（各機不同）
.claude/settings.local.json

# 相依套件
# ⚠️ 不可寫成 `node_modules/`（帶斜線＝只比對「目錄」）：worktree 會把 node_modules 做成
# **symlink** 指回主目錄，而 symlink 對 Git 來說不是目錄 → 不帶斜線才擋得住
#（2026-07-19 實際踩到：symlink 被 git add -A 收進 commit，還把本機絕對路徑一起帶上去）
node_modules

# 產生的檔案
# 審查產出的殘留目錄（早期 review-loop 工具留下的；工具已不收，見 審查與合併程序.md）
.codex-reviews/
tmp/
output/
*.log

# 系統
.DS_Store
**/.DS_Store

# ── 本機工具與紀錄：留著但不進版控（William 2026-08-03 裁決）──
# ⚠️ 這些是**未追蹤**檔，Git 沒有備份 ⇒ 刪掉就真的沒了。所以是「忽略」不是「刪除」。
#    目的不是把東西藏起來，是讓 `git status` 只剩下**真的有意義的改動**——
#    永遠有五行 `??` 的話，哪天真的多出一個不該在的檔案，沒有人會注意到。
_local/
.agents/
skills-lock.json
# ⚠️ 這三個先在原地忽略；William 決定要不要搬進 _local/ 之後再調整。
#    （2026-08-03 踩過：`git add -A` 會把未追蹤檔一起收進 commit，
#      正好違反「工具不收」的裁決——是 push 守門的考題擋下來的。）
scripts/review-loop.js
meeting.sh
meeting_0724_1414.md
