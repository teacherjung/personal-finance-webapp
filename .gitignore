# 個人實際資料與憑證（含 IBKR flexToken、帳戶餘額）——絕不進版控
data/store.json
data/store.json.bak
data/store.db
data/store.db.bak
data/*.db
data/*.db-wal
data/*.db-shm
data/store.manual-backup-*.json
data/*backup*
*.bak

# Claude Code 本機設定（各機不同）
.claude/settings.local.json

# 相依套件
# ⚠️ 不可寫成 `node_modules/`（帶斜線＝只比對「目錄」）：worktree 會把 node_modules 做成
# **symlink** 指回主目錄，而 symlink 對 Git 來說不是目錄 → 不帶斜線才擋得住
#（2026-07-19 實際踩到：symlink 被 git add -A 收進 commit，還把本機絕對路徑一起帶上去）
node_modules

# 產生的檔案
# 審查產出的殘留目錄（早期 review-loop 工具留下的；工具已不收，見 REVIEW-AND-MERGE.md）
.codex-reviews/
tmp/
output/
*.log

# 系統
.DS_Store
**/.DS_Store

# ── 工具產生的本機設定，與 William 個人的檔案（2026-08-03 裁決）──
# 目的是讓 `git status` 只剩下**真的有意義的改動**——永遠有五行 `??` 的話，
# 哪天真的多出一個不該在的檔案，沒有人會注意到。
#
# ⛔ **這一節不是「沒用到的東西的收容所」。** 我們自己寫了但沒在用的程式碼，
#    規則是**直接刪**（AGENTS.md「沒用到的程式直接刪」），**不可以靠加一條忽略規則藏起來**。
#    2026-08-03 我曾經加過 `/retired/`（外加 `eslint.config.js` 的同名豁免），
#    等於一邊立規則說不准藏、一邊把藏身處做得比刪除更順手；Codex #387 r4 抓到後拆掉。
#    **要加新的一行進來以前先問：這是工具／別人產生的，還是我寫的死程式碼？後者一律刪。**
#
# ⚠️ 前面加 `/` ＝**只比對 repo 根目錄**（Codex #387 r1）：
#    不加的話會匹配任意深度的同名項目——例如將來某處出現 `lib/meeting.sh`，
#    它會被靜靜忽略掉，而那不是這幾條想擋的東西。
/.agents/
/skills-lock.json
/meeting.sh
/meeting_0724_1414.md
