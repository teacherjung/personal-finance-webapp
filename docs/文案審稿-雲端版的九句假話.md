# 文案審稿：雲端版的九句假話

> **這份要 William 審。** Claude 起草、老師審改（專案鐵則）。
> 建立 2026-07-29。改完之後由 Claude 實作，**不要直接改程式**——有些句子牽涉模式判斷。

## 這份在講什麼

app 現在有兩種跑法：

- **本機版**：資料在你的 Mac 上（`data/store.db`）
- **雲端版**：資料在 Supabase，別人也會用

問題是**畫面上有九句話是照「只有本機版」的年代寫的**，在雲端版全部變成假話。
使用者是在「要不要把身分證字號交出來」「要不要按下不可逆的整批改名」這種時刻讀到它們的。

分兩類，**第二類比第一類嚴重**。

---

## 第一類：隱私承諾（5 句）

說「只存這台電腦」，但雲端版其實存在 Supabase。

| # | 位置 | 現在的話 | 在雲端版為什麼是假的 |
|---|---|---|---|
| 1 | 卡片編輯窗 | 帳單 PDF 密碼（**只存這台電腦、永不上傳**） | 會上傳，存在雲端資料庫（**有加密**） |
| 2 | 設定頁·證券 | …只存這台電腦、永不上傳，比照信用卡帳單密碼 | 同上 |
| 3 | 資產頁·帳戶 | 完整帳號（選填，**只存這台電腦**） | 會上傳，而且**沒有加密**（你 2026-07-29 裁決先不加密）。備份檔已不夾帶完整帳號（#338 已於 2026-07-30 合併） |
| 4 | 設定頁·備份 | 所有資料**只存在本機** `data/store.db` | 雲端版資料在 Supabase |
| 5 | 收支頁·上傳 | 對帳單密碼（只在這台電腦解密、不會上傳、**不會儲存**） | ⚠️ **這一句其實是真的**——那個欄位是每次上傳現填、用完就丟。只有「這台電腦」的講法要調整 |

### 草稿

**1. 卡片 PDF 密碼**
- 本機版：`帳單 PDF 密碼（只存這台電腦、永不上傳）` ← 不動
- 雲端版：`帳單 PDF 密碼（加密後存在你的帳號裡，只有你看得到）`

**2. 證券對帳單密碼**
- 本機版：不動
- 雲端版：`存起來後上傳對帳單就不用每次輸入。密碼會加密後存在你的帳號裡。不存也可以——每次上傳時再輸入。`

**3. 完整帳號** ⚠️ 這一句最需要你決定語氣

> ⚠️ **時序提醒（2026-07-31 更新）**：#338 已於 2026-07-30 合併——底下草稿的前提
> 「備份檔不夾帶完整帳號」**已成立**，這幾句文案**可以實作了**（何時做由 William 排）。
- 本機版：`完整帳號（選填，只存這台電腦）` ← 不動
- 雲端版（誠實版）：`完整帳號（選填，存在你的帳號裡；用來把銀行對帳單對到這個帳戶）`
- 雲端版（更誠實版）：`完整帳號（選填，存在你的帳號裡、未加密；只用來把銀行對帳單對到這個帳戶）`

> **為什麼要你選**：你裁決了「先不加密」。要不要把「未加密」三個字寫出來，是產品判斷——
> 寫了比較誠實，但也可能讓人不敢填（不填的話銀行對帳單就配不到帳戶）。
> 我的建議是**不寫「未加密」**，因為對一般使用者來說那三個字只會製造焦慮、
> 卻沒有可行動的意義（他不能選擇加密）。但這是你的決定。

**4. 設定頁·備份說明**
- 本機版：不動
- 雲端版：`你的資料存在雲端資料庫，每天自動備份。建議偶爾自己匯出一份存著（匯出格式為 JSON）。`
  - ⚠️ **一定要補一句**：雲端版匯出的檔案**不含** IB 憑證與 PDF 密碼（刻意的），
    還原之後那幾樣要重新輸入一次。不講的話，使用者會以為「我有備份」。
  - 建議完整版：`你的資料存在雲端資料庫，每天自動備份。你也可以自己匯出一份存著——但匯出的檔案不含 IB 憑證與帳單密碼（為了安全刻意拿掉），還原後要重新輸入一次。`

**5. 收支頁上傳密碼** — 只改「這台電腦」的講法
- 兩種模式共用：`對帳單密碼（只用來解開這次上傳的檔案，不會儲存）`

---

## 第二類：安全網承諾（4 句）⚠️ 這一類比較嚴重

說「儲存前會自動備份」，但**雲端版根本沒有備份這個動作**
（`lib/repo.js` 的 `backupNow()`：雲端模式直接回 `false`，不備份、也不報錯——行號會漂所以不寫死，2026-08-05 覆核在 252 行。結論不變＝這一類文案仍是真問題）。

**為什麼比第一類嚴重**：使用者是在按下**不可逆**的動作之前讀到這句話的——
整批改名、刪分類、套用店名規則到所有舊記錄。他會想「反正有備份」然後按下去。

| # | 位置 | 現在的話 |
|---|---|---|
| 6 | 設定頁·支出分類 | ~~儲存前自動備份~~ → **2026-08-05 已改（#410 r9）**，見下框 |
| 7 | 設定頁·收入分類 | 同上 |
| 8 | 設定頁·店名規則 | ~~套用前會另存一份還原檔 `data/store.db.pre-rules.bak`~~ → **2026-08-05 已改（#410 r10）**，見下框 |
| 9 | 店名規則面板 | ~~規則對**全部**記錄生效…**儲存前自動備份**~~ → **同上（r10）** |

> ### 🧑‍⚖️ 6／7 已結案（2026-08-05，William 拍板）
> 稽核（#410）查出比「雲端版沒備份」更嚴重的事：**連本機版都沒有**——
> `lib/services/categories.js` 全檔 `backupNow` 出現 0 次，所以 6／7 這兩句**在本機版也是假話**
> （8 是真的、9 走同一條 `pre-rules`，兩者都成立）。
>
> 兩條路攤給 William 選（實測資料：他的開機備份 `store.db.bak` 當時只到一天多前；
> 補備份的代價是每次存分類多寫約 560KB、同名覆蓋不累積）：
> **他選「只改文案，不動程式」**——理由是他自己知道要在大改動前先重啟一次拿新備份，
> 不值得為此讓每次存分類都多寫一份。
>
> ⚠️ **第一版改法自己又是一句假話**（#410 r8 複驗抓到）：當時寫的是
> 「改動前先重啟一次後端，每次啟動都會另存一份當下的還原檔 `data/store.db.bak`」——但
> `public/modules/settings.js` 兩種模式**共用同一份、沒有分流**，而**雲端版壓根不會產生那顆檔**
> （`lib/repo.js` 的 `backupNow` 在雲端一律回 `false`）；就算在本機版，那顆啟動備份也是 best-effort
> （磁碟滿／權限／`VACUUM` 失敗時只寫後端 `console.warn`，照常啟動、前端完全不知道，
> 還保留可能過期的舊 `.bak`）。前端目前也**拿不到「本機還是雲端」這個資訊**
> （`public/` 底下零命中 `hosted`），要分流就得新增 API 欄位＝擴大範圍，違反「只改文案」的裁決。
>
> **定稿（#410 r9 已上，兩種模式共用同一句）**：
> `⚠️ 這個動作不會自動備份，存下去也沒有「復原」可以按——動手前請先到本頁最下面「資料與備份」按「匯出備份」存一份（真要救的時候，用旁邊的「匯入備份」整包還原）。`
>
> 為什麼這句撐得住挑剔：①**不承諾任何「系統會自動幫你做」**，只請使用者自己按一個按鈕
> ②指的按鈕**兩種模式都有**（`public/modules/settings.js` 的 `/api/export` 與 `importBtn`，
> 就在同一頁最下面「資料與備份」）③「沒有『復原』可以按」是 UI 事實（分類編輯器只有「取消」與
> 「儲存分類」）④「整包還原」也是事實（`lib/routes/core.js` 的匯入走 `{ overwrite: true }`——
> 講明它才不會讓人以為那是「只還原分類」的針對性復原）。
> 匯出檔的完整性查證過：本機版含機密、雲端版剝掉機密與完整帳號，但**還原回自己的帳號時現值會保留**
> （`test/hosted-secrets.test.js` 來回①②），所以這句話**不必**再補一段機密的但書。
>
> 撐這句話的考題＝`test/vault-and-backup-integrity.test.js` 第二節的**三題**（r10 補齊，見下框）。
>
> ⚠️ **不要有人「順手把備份補回來」**：那是被否決的方案，不是遺漏。要改請先問 William。

> ### 🔧 8／9 已修事實層（2026-08-05 #410 r10）——⏳ **措辭仍待 William 審**
> r9 複驗抓到兩件事，都不在那句話本身：
>
> **①「同一頁還有一句沒改的同族假話」**：r9 只改了 6／7，同一頁的店名規則卡片還寫著
> 「（套用前會另存一份還原檔 `data/store.db.pre-rules.bak`）」、店名規則面板還寫著「儲存前自動備份。」。
> 這兩句在**雲端版都是假的**（`backupNow` 在 HOSTED 一律回 `false`），而且 r9 自己剛證明過
> 「前端拿不到模式資訊、不能分流」——所以留著就是同一個病的第二、第三份。
> 本機版那一半是**真的**（`pre-rules` 有考題釘住），所以改法不是刪掉，而是**讓句子自己交代模式**：
>
> - **8（設定頁·店名規則卡片）定稿**：
>   `改完先預覽影響再儲存，儲存後立刻套用到所有舊記錄。⚠️ 存下去沒有「復原」可以按——動手前請先到本頁最下面「資料與備份」按「匯出備份」存一份（app 跑在自己電腦上時，系統另外會自己存一份還原檔 data/store.db.pre-rules.bak，但那是盡力而為、失敗了畫面不會講；跑在雲端就沒有這一份，別只靠它）。`
> - **9（店名規則面板）定稿**：
>   `規則對全部記錄生效，所以請先按「預覽影響」看看會改到哪些，確認沒問題再儲存。⚠️ 存下去沒有「復原」可以按，別只靠系統的自動備份（app 跑在自己電腦上才有那一份，而且失敗了畫面不會講）——動手前請先到設定頁最下面「資料與備份」按「匯出備份」存一份。`
>
> 「盡力而為、失敗了畫面不會講」不是客套：`store-rules.js` 把 `backupNow` 的回傳值丟掉，
> 備份失敗時畫面照樣只顯示「儲存成功」（本節第一個 ⚠️ 記的就是這個缺口）。
>
> **②「守它的考題留了一個縫」**：r9 那一題只證明「兩顆按鈕還在」，而文案真正的承諾是**按下去救得回分類**。
> 實測（r9 那棵樹）：把雲端匯出的投影 `stripSecretsForBackup` 加三行刪掉 `expenseTree/incomeTree/categoryAliases`
> ⇒ `npm test` **1498 pass / 0 fail**，無一題轉紅＝雲端使用者照文案存下來的檔案裡沒有分類樹，
> 「匯入備份」還原回去分類靜靜退回系統預設。r10 補了一題釘住它（含「雲端的 `/api/export` 真的走這支投影」的接線）。
>
> r10 之後撐這一族的三題（都在 `test/vault-and-backup-integrity.test.js` 第二節）：
> ①兩顆按鈕還在同一頁 ＋ 文案不可再長出假承諾／不可指向本機專屬的東西；
> ②雲端匯出的檔案裡必須有分類樹；③**整個 `public/` 的掃描題**——任何提到「自動備份／還原檔／.bak」
> 的畫面文案，都必須自己交代是哪一種模式才有（或是否定句）。下一個人在新頁面寫「儲存前自動備份」，第③題會在他手上轉紅。
>
> ⏳ **要 William 決定的三件小事**（都不影響上面兩句的事實正確性）：
> 1. 8／9 的措辭要不要更白話（「app 跑在自己電腦上」是我選的講法，避免在畫面上冒出「本機版／雲端版」這種術語）。
> 2. 「匯出備份」按鈕按下去**沒有任何成功回饋**（它是純 `<a download>`，不像其他動作有 toast）。
>    雲端版若剛好 session 過期，瀏覽器會安靜地存下一個內容是錯誤訊息的檔案，而使用者以為自己有備份了。
>    這是**既有行為**（不是這幾版弄出來的），但新文案把使用者的自保完全押在這顆鈕上——
>    要不要補一句「存完打開看一眼，裡面應該是一大串資料而不是錯誤訊息」？
> 3. 文案引的是「匯出備份」，畫面上那顆的字面是「匯出備份 (JSON)」（前綴關係，照著找得到）。
>    維持現狀還是把括號拿掉？考題已釘住「按鈕字面必須以『匯出備份』開頭」，改名會轉紅。

### 草稿

**6／7（分類管理）**——**兩種模式都已結案**，定稿見上框。r8 那版還留著「雲端版仍待處理」，
是因為它把使用者導向只有本機才有的 `.bak`；r9 定稿改成導向兩邊都有的「匯出備份」按鈕，
所以不需要、也不應該再另擬一句雲端版（同一句話兩種版本＝下一個漂移副本）。

**8／9（店名規則的還原檔與面板）**——事實層已於 r10 修好，定稿見上框。
~~原草稿是「本機版不動、雲端版另寫一句」~~：**那條路已經走不通**，因為 r9 查明前端拿不到模式資訊
（`grep hosted public/` 零命中），要分流就得新增 API 欄位＝擴大範圍。所以改成**同一句話自己交代模式**，
與 6／7 同一個原則：畫面上只有一份 HTML，那份 HTML 說的話必須在兩種模式下都是真的。

> ⚠️ **同族但不同門**：第一類的 #4（設定頁·備份「所有資料只存在本機 `data/store.db`」）與
> `public/index.html` 頁尾的「資料存於本機 `data/store.db`」**仍未改**——那屬第一類，等 William
> 定語氣（#4 的草稿還牽涉「雲端匯出不含機密」的但書）。r10 的掃描題**刻意不涵蓋**它們
>（觸發字只有「自動備份／還原檔／.bak」），所以**那兩句目前沒有任何考題盯著**，不要以為掃描題全包了。

> 💡 **我的建議**：這一類**不要等其他文案一起改**。就算你對第一類還沒想好，
> 第二類也該先修——因為它會讓人在錯誤的安全感下按不可逆的按鈕。

---

## 三件事的裁決（William 2026-07-29，實作照這個走）

| | 問題 | 裁決 |
|---|---|---|
| ① | 要不要提「加密」這些字 | **B：提加密**，但完整帳號那句**不寫「未加密」**（他不能選擇加不加密，寫了只製造焦慮） |
| ② | 要不要配可展開的詳細說明 | **B：句子＋詳解連結**（`.info-link`／`openInfo`；至少「帳號」與「備份」兩處） |
| ③ | 「別的使用者看不到你的資料」現在寫不寫 | **同意先不寫，驗收過再寫** |

### ✅ ③ 的條件已於同日達成——那句話現在可以寫了

C6 對抗審查 2026-07-29 在真部署上跑出 **16 題全過、0 失敗**，
其中第⑤題是**拿 A 的 access token 直接打 Supabase REST**（繞過我們的伺服器）：
A 只看到自己的 21 列，想插一列 `user_id=B` 被 RLS 的 `WITH CHECK` 拒掉。
紀錄在 `docs/C6-部署與對抗審查-操作手冊.md` §五。

所以實作時**要加上**那句——但只寫已經驗過的口徑，例如
「**你的資料只有你自己看得到；別的使用者連資料庫層都讀不到**」，
不要延伸成「絕對安全」「無法被駭」這類沒有驗收撐著的話。

> ⚠️ 這句話的效期綁在那份驗收上。**RLS 是任何一支 PR 都可能悄悄弄壞的東西**，
> 所以規矩是：改動 auth／RLS／匯入／機密欄位的 PR 合併後重跑 C6。
> 哪天 C6 紅了，這句文案就要跟著下架——**不是先改文案，是先修好**。

---

## 原始提問（保留備查）

### ① 語氣：要不要提「加密」「未加密」這些字？

- **A. 完全不提技術**：「存在你的帳號裡，只有你看得到」
- **B. 提加密**：「加密後存在你的帳號裡」（我草稿用的）
- **C. 連未加密也講**：完整帳號那句寫出「未加密」

我建議 **B**：對「身分證字號」這種東西，講「有加密」是使用者真的想知道的；
但帳號那句不提「未加密」（見上面的理由）。

### ② 要不要加「可展開的詳細說明」？

專案有一條鐵則：「懂了才不會誤會的概念，一律在網頁上就地白話解釋」。
資料放哪、誰看得到，算不算那種概念？

- **A. 只改句子**（最小改動）
- **B. 句子 ＋ 一個「詳細說明」連結**，點開講清楚「資料在哪、誰看得到、我們做了什麼保護」

我**沒有替你決定**——那條鐵則原文限定在「否則會把正常數字當成算錯」的概念，
資料放哪不完全屬於那一類。這是你的產品判斷。

### ③ ⚠️ 一句我建議現在**不要寫**的話 —— ✅ 條件已達成，見上面

> 這一節是提問當下（2026-07-29 上午）的狀態。**同日下午 C6 已跑過真 Supabase 並全過**，
> 條件解除，那句話可以寫了。保留原文是為了記住判準：**沒驗過的保證不寫**。

草稿裡我刻意**沒有**寫「別的使用者看不到你的資料」這種話，即使那是我們的設計目標。

原因：那件事**還沒被證明**。跨使用者隔離目前只有「模擬資料庫」的考題，
真正算數的驗收（拿 A 的登入憑證直接打資料庫讀 B 的資料）**要等 C6 對抗腳本跑過真的 Supabase**——
而那支腳本今天才剛修好兩個假綠。

**在那之前，文案只寫已經證明的部分**（要登入才進得來），不寫資料庫層的隔離。
等驗收過了再加，那時候是有憑有據的。

---

## 技術附註（給 Claude 自己，你不用看）

前端目前**完全不知道自己跑在哪個模式**（`grep hosted public/` 零命中），所以「依模式顯示不同句子」需要先牽一條線。兩個選項：

- **A. 後端加旗標**：`/api/settings` 回應多一個布林。⚠️ 必須**只在 HOSTED 送出**，否則會改到 LOCAL 的 API 回應形狀，撞到「LOCAL 零改動」契約。
- **B. 用網址判斷**：LOCAL 把 SPA 掛在 `/`、HOSTED 掛在 `/finance`，所以 `location.pathname` 現成可用、零後端改動。比較 hacky，但不會碰到契約。

實作時一併補考題：**九處文案在 HOSTED 模式下不得出現「只存」「這台電腦」「自動備份」**。

> 📌 進度（2026-08-05 #410 r10）：這條「補考題」目前只做到**「自動備份／還原檔／.bak」那一族**——
> `test/vault-and-backup-integrity.test.js` 第二節的掃描題會走遍整個 `public/`，
> 要求每一句都自己交代模式或是否定句。**「只存」「這台電腦」那一族還沒有**（第一類尚未定稿）。
> 也就是說：上面那句話還不是事實，是還沒做完的計畫——別把它當成已完成的保護。
