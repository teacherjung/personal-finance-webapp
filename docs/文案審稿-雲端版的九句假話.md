# 文案審稿：雲端版的九句假話

> **這份要 William 審。** Claude 起草、老師審改（專案鐵則）。
> 建立 2026-07-29。改完之後由 Claude 實作，**不要直接改程式**——有些句子牽涉模式判斷。

## 這份在講什麼

app 現在有兩種跑法：

- **本機版**：資料在你的 Mac 上（`data/store.db`）
- **雲端版**：資料在 Supabase，別人也會用

問題是**畫面上有九句話是照「只有本機版」的年代寫的**，在雲端版全部變成假話。
使用者是在「要不要把身分證字號交出來」「要不要按下不可逆的整批改名」這種時刻讀到它們的。

分兩類，**第二類比第一類嚴重**。

---

## 第一類：隱私承諾（5 句）

說「只存這台電腦」，但雲端版其實存在 Supabase。

| # | 位置 | 現在的話 | 在雲端版為什麼是假的 |
|---|---|---|---|
| 1 | 卡片編輯窗 | 帳單 PDF 密碼（**只存這台電腦、永不上傳**） | 會上傳，存在雲端資料庫（**有加密**） |
| 2 | 設定頁·證券 | …只存這台電腦、永不上傳，比照信用卡帳單密碼 | 同上 |
| 3 | 資產頁·帳戶 | 完整帳號（選填，**只存這台電腦**） | 會上傳，而且**沒有加密**（你 2026-07-29 裁決先不加密）。備份檔已不夾帶完整帳號（#338 已於 2026-07-30 合併） |
| 4 | 設定頁·備份 | ~~所有資料**只存在本機** `data/store.db`~~ ✅ 那句已於 #417（2026-08-06）改成中性版，見下方草稿區 | 雲端版資料在 Supabase |
| 5 | 收支頁·上傳 | 對帳單密碼（只在這台電腦解密、不會上傳、**不會儲存**） | ⚠️ **這一句其實是真的**——那個欄位是每次上傳現填、用完就丟。只有「這台電腦」的講法要調整 |

### 草稿

**1. 卡片 PDF 密碼**
- 本機版：`帳單 PDF 密碼（只存這台電腦、永不上傳）` ← 不動
- 雲端版：`帳單 PDF 密碼（加密後存在你的帳號裡，只有你看得到）`

**2. 證券對帳單密碼**
- 本機版：不動
- 雲端版：`存起來後上傳對帳單就不用每次輸入。密碼會加密後存在你的帳號裡。不存也可以——每次上傳時再輸入。`

**3. 完整帳號** ⚠️ 這一句最需要你決定語氣

> ⚠️ **時序提醒（2026-07-31 更新）**：#338 已於 2026-07-30 合併——底下草稿的前提
> 「備份檔不夾帶完整帳號」**已成立**，這幾句文案**可以實作了**（何時做由 William 排）。
- 本機版：`完整帳號（選填，只存這台電腦）` ← 不動
- 雲端版（誠實版）：`完整帳號（選填，存在你的帳號裡；用來把銀行對帳單對到這個帳戶）`
- 雲端版（更誠實版）：`完整帳號（選填，存在你的帳號裡、未加密；只用來把銀行對帳單對到這個帳戶）`

> **為什麼要你選**：你裁決了「先不加密」。要不要把「未加密」三個字寫出來，是產品判斷——
> 寫了比較誠實，但也可能讓人不敢填（不填的話銀行對帳單就配不到帳戶）。
> 我的建議是**不寫「未加密」**，因為對一般使用者來說那三個字只會製造焦慮、
> 卻沒有可行動的意義（他不能選擇加密）。但這是你的決定。

**4. 設定頁·備份說明**

> ✅ **已部分實作（#417，2026-08-06）**：本機版那句「所有資料只存在本機 `data/store.db`（SQLite）」
> **已改成中性版**——`public/modules/settings.js` 的 `BACKUP_CARD_NOTE`：
> 「可以把你的資料匯出成一個檔案（JSON 檔）存起來。建議定期匯一份、放到你自己找得到的地方——
> 要還原的時候用「匯入備份」讀回來。」
> 理由：那支 PR 正在改這張卡（匯出鈕改成會出聲），不能一邊加雲端相關訊息、一邊留著一句在雲端是假的
> 資料存放說明。前端拿不到模式資訊，所以**不分流**，改成兩種模式都成立的說法。
>
> ⚠️ **那句話改過兩次**（第二次是同一支 PR 的 r2 審查抓的）：中性版第一稿寫「把你的資料**整包**
> 匯出成一個檔案」——在雲端版**同樣是假的**，只是換了病灶：舊句錯在「資料放哪」、那句錯在
> 「檔案裡有什麼」（HOSTED 匯出走 `stripSecretsForBackup`，刻意剝掉 IB 憑證／帳單 PDF 密碼／
> 證券對帳單密碼／完整帳號）。而下面那一行自己就寫著「不講的話，使用者會以為『我有備份』」——
> 「整包」正好把那個誤解講死。所以「整包」已刪掉，並加了考題釘住
> （`test/backup-export.test.js`〈文案｜「資料備份」卡的說明不可以宣稱「整包／全部／完整」〉：
> 那幾個宣稱內容完整的詞一寫回去就轉紅）。
> ⚠️ **完整的雲端語氣裁決仍待 William**：下面這幾句（尤其「雲端匯出不含 IB 憑證與帳單密碼」那句
> 一定要補、以及要不要配 `.info-link` 詳解）都還沒實作，需要模式分流才寫得出來。
> **刻意先不補**：用一個含糊詞蓋過去比少講一句更糟——寧可不講，也不要講一句在雲端是假的話。

- 本機版：~~不動~~ → 已改中性版（見上面的方框）
- 雲端版：`你的資料存在雲端資料庫，每天自動備份。建議偶爾自己匯出一份存著（匯出格式為 JSON）。`
  - ⚠️ **一定要補一句**：雲端版匯出的檔案**不含** IB 憑證與 PDF 密碼（刻意的），
    還原之後那幾樣要重新輸入一次。不講的話，使用者會以為「我有備份」。
  - 建議完整版：`你的資料存在雲端資料庫，每天自動備份。你也可以自己匯出一份存著——但匯出的檔案不含 IB 憑證與帳單密碼（為了安全刻意拿掉），還原後要重新輸入一次。`

**5. 收支頁上傳密碼** — 只改「這台電腦」的講法
- 兩種模式共用：`對帳單密碼（只用來解開這次上傳的檔案，不會儲存）`

---

## 第二類：安全網承諾（4 句）⚠️ 這一類比較嚴重

原本這四句說「儲存前會自動備份」「套用前會另存一份還原檔」，但**雲端版根本沒有備份這個動作**
（`lib/repo.js` 的 `backupNow()`：雲端模式直接回 `false`，不備份、也不報錯——行號會漂所以不寫死，
2026-08-05 覆核在 252 行）。更糟的是稽核查出 6／7 **連本機版都沒有**
（`lib/services/categories.js` 全檔 `backupNow` 出現 0 次）。

**為什麼比第一類嚴重**：使用者是在按下**不可逆**的動作之前讀到這句話的——
整批改名、刪分類、套用店名規則到所有舊記錄。他會想「反正有備份」然後按下去。

> ### ⏹ 現況（2026-08-06 收斂成單一時態；四句都已改成事實）
>
> 畫面上現在的字，**逐字**如下（這是唯一有效的版本，以前各版一律只當沿革讀）：
>
> | # | 位置 | 現在畫面上的字 |
> |---|---|---|
> | 6 | 設定頁·支出分類 | `⚠️ 儲存後沒有「復原」可以按。` |
> | 7 | 設定頁·收入分類 | 同上（兩段共用同一句） |
> | 8 | 設定頁·店名規則卡片 | `改完先預覽影響再儲存，儲存後立刻套用到所有舊記錄。⚠️ 存下去沒有「復原」可以按——動手前請先到本頁最下面「資料與備份」按「匯出備份」存一份（本機版另外會自己存一份還原檔 data/store.db.pre-rules.bak，但那是盡力而為、失敗了畫面不會講；雲端版沒有這一份，別只靠它）。` |
> | 9 | 店名規則面板 | `規則對全部記錄生效，所以請先按「預覽影響」看看會改到哪些，確認沒問題再儲存。⚠️ 存下去沒有「復原」可以按，別只靠系統的自動備份（只有本機版才有那一份，而且失敗了畫面不會講）——動手前請先到設定頁最下面「資料與備份」按「匯出備份」存一份。` |
>
> 這四句現在成立的三件事：
> 1. **6／7 不承諾任何備份，也不指路**——只留一句不可逆警告（那兩段兩種模式都沒有備份可承諾）。
> 2. **8／9 自己交代模式**（`本機版`／`雲端版`）——因為 `public/` 兩種模式共用同一份 HTML
>    （`grep hosted public/` 零命中＝前端拿不到模式資訊），句子只能自己講清楚哪一邊才有。
> 3. **「沒有『復原』可以按」是 UI 事實**：分類編輯器只有「取消」與「儲存分類」，店名規則編輯窗
>    只有「取消／預覽影響／儲存並套用」。指路指到的兩顆鈕（`/api/export` 與 `importBtn`）
>    **兩種模式都有**，就在設定頁最下面「資料與備份」。
>
> ⏳ **待 William 過目一句**：6／7 那句是 2026-08-06 依複驗者意見收窄的
>（原本是「⚠️ 儲存後無法復原。」）。理由見下方沿革 r6；他可以一句話改回去。

> ### 沿革（誰裁決的、之前哪一版錯在哪）——只當歷史讀，不要引用成定稿
>
> | 版本 | 那一版寫什麼 | 錯在哪／為什麼改 | 誰決定 |
> |---|---|---|---|
> | 原版 | 「儲存前自動備份。」（6／7／9）、「套用前會另存一份還原檔 `data/store.db.pre-rules.bak`」（8） | 雲端版沒有備份這個動作；6／7 連本機版都沒有 | 稽核 #410 查出 |
> | r8 | 「改動前先重啟一次後端，每次啟動都會另存 `data/store.db.bak`」 | **自己又是一句假話**：雲端版不會產生那顆檔；本機版也只是 best-effort（VACUUM／磁碟／權限失敗只寫後端 `console.warn`，畫面完全不知道）；而且叫雲端使用者去操作他沒有的後端 | #410 r8 複驗退回 |
> | r9／r10 | 「不會自動備份…到本頁最下面『資料與備份』按『匯出備份』存一份（真要救的時候用旁邊的『匯入備份』把整包資料換回存檔那一刻）」 | 事實正確，但 **William 判定太囉嗦**（6／7 那兩段） | William 2026-08-05 |
> | r10 用詞 | 「app 跑在自己電腦上」 | 改用 **「本機版／雲端版」**（他自己平常就這樣講，更省字；未來若有其他使用者再議）——已落實在 8／9 | William 2026-08-05 |
> | r11 | 6／7 只留「⚠️ 儲存後無法復原。」 | 短，但**不精確**：既有的匯出檔可以整包匯回去（`/api/import` 走 `overwrite`），所以「無法復原」是無限定的世界斷言 | #410 r6 複驗指出 |
> | r6（**現況**） | 6／7 改成「⚠️ 儲存後沒有「復原」可以按。」 | 同樣短（只多一個字），但只斷言撐得住的 UI 事實 | Claude 依複驗意見改，**待 William 過目** |
>
> 🧑‍⚖️ **William 2026-08-05 的產品裁決（不要重議）**：**不為分類管理補備份，只改文案**。
> 理由是他自己知道要在大改動前先重啟一次拿新備份，不值得為此讓每次存分類都多寫一份
>（實測資料：他的開機備份 `store.db.bak` 當時只到一天多前；補備份的代價是每次存分類多寫約 560KB、同名覆蓋不累積）。
> ⚠️ **不要有人「順手把備份補回來」**：那是被否決的方案，不是遺漏。要改請先問 William。

> ### 🧪 撐這四句的考題（都在 `test/vault-and-backup-integrity.test.js` 第二節）
> 1. **分類管理那句警告不可被靜靜刪掉**，而且那兩段不可再長出備份承諾／不可指向只有本機才有的
>    檔案路徑；文案叫使用者「按『某某』」的名字，畫面上要真的有那顆鈕（不寫死名單、先去註解、
>    以一段 `<p>` 文案為單位）。
> 2. **雲端版匯出的檔案裡必須有分類樹**（`stripSecretsForBackup` 不可把 `expenseTree`／`incomeTree`／
>    `categoryAliases` 一起剝掉），含「雲端的 `/api/export` 真的走這支投影」的接線。
> 3. **整個 `public/` 的掃描題**：任何「系統會替你留一份」的說法都要自己交代模式，或本身是否定句。
>    2026-08-06 重新設計過——舊版只認三個字（自動備份／還原檔／.bak）、否定詞含裸字「沒」、
>    又整檔略過 `backup-alert.js`，三種明確假承諾都能通過（「沒問題，會自動備份。」／
>    「系統每次都會替你留一份可恢復的副本。」／寫在 `backup-alert.js` 裡的假承諾）。
>
> ⚠️ **文字掃描擋不完**（誠實劃界）：第 3 題擋的是「照現在的寫法再寫一句同族假話」，不是所有可能的
> 假承諾（例如換一組完全不同的詞）。要更強的保證只能把模式資訊真的送到前端（見文末技術附註）。

> ### ⏳ 還沒做完的兩件事（不影響上面四句的事實正確性）
> 1. **〈匯出備份〉那顆鈕沒有任何成功／失敗回饋**（它是純 `<a download>`，不像其他動作有 toast）。
>    雲端版若剛好 session 過期，瀏覽器會安靜地存下一個內容是錯誤訊息的檔案，而使用者以為自己有備份了。
>    這是**既有行為**（不是這幾版弄出來的）。**William 2026-08-05 裁決：不用文案打補丁，另開一支真的修**
>    ——那一支是 `fix/export-backup-feedback`。
>    ⚠️ **合併順序有意義**：8／9 那句「動手前先按〈匯出備份〉存一份」的可信度**依賴那顆鈕會回報成功／失敗**；
>    在 `fix/export-backup-feedback` 合併之前，這句指路的保護力有缺口（考題只證明得出
>    「鈕在、名字對得上、匯出的內容含分類樹」，證明不出「使用者真的拿到一份完整的檔案」）。
> 2. 文案引的是「匯出備份」，畫面上那顆的字面是「匯出備份 (JSON)」（前綴關係，照著找得到）。
>    維持現狀還是把括號拿掉？考題已釘住「按鈕字面必須以『匯出備份』開頭」，改名會轉紅。

> ⚠️ **同族但不同門**：第一類的 #4（設定頁·備份「所有資料只存在本機 `data/store.db`」）與
> `public/index.html` 頁尾的「資料存於本機 `data/store.db`」**仍未改**——那屬第一類，等 William
> 定語氣（#4 的草稿還牽涉「雲端匯出不含機密」的但書）。第 3 題掃描題**刻意不涵蓋**它們
>（它問的是「系統會不會替你留一份」，不是「資料存在哪」），所以**那兩句目前沒有任何考題盯著**。

> 💡 **我的建議**：這一類**不要等其他文案一起改**。就算你對第一類還沒想好，
> 第二類也該先修——因為它會讓人在錯誤的安全感下按不可逆的按鈕。

---

## 三件事的裁決（William 2026-07-29，實作照這個走）

| | 問題 | 裁決 |
|---|---|---|
| ① | 要不要提「加密」這些字 | **B：提加密**，但完整帳號那句**不寫「未加密」**（他不能選擇加不加密，寫了只製造焦慮） |
| ② | 要不要配可展開的詳細說明 | **B：句子＋詳解連結**（`.info-link`／`openInfo`；至少「帳號」與「備份」兩處） |
| ③ | 「別的使用者看不到你的資料」現在寫不寫 | **同意先不寫，驗收過再寫** |

### ✅ ③ 的條件已於同日達成——那句話現在可以寫了

C6 對抗審查 2026-07-29 在真部署上跑出 **16 題全過、0 失敗**，
其中第⑤題是**拿 A 的 access token 直接打 Supabase REST**（繞過我們的伺服器）：
A 只看到自己的 21 列，想插一列 `user_id=B` 被 RLS 的 `WITH CHECK` 拒掉。
紀錄在 `docs/C6-部署與對抗審查-操作手冊.md` §五。

所以實作時**要加上**那句——但只寫已經驗過的口徑，例如
「**你的資料只有你自己看得到；別的使用者連資料庫層都讀不到**」，
不要延伸成「絕對安全」「無法被駭」這類沒有驗收撐著的話。

> ⚠️ 這句話的效期綁在那份驗收上。**RLS 是任何一支 PR 都可能悄悄弄壞的東西**，
> 所以規矩是：改動 auth／RLS／匯入／機密欄位的 PR 合併後重跑 C6。
> 哪天 C6 紅了，這句文案就要跟著下架——**不是先改文案，是先修好**。

---

## 原始提問（保留備查）

### ① 語氣：要不要提「加密」「未加密」這些字？

- **A. 完全不提技術**：「存在你的帳號裡，只有你看得到」
- **B. 提加密**：「加密後存在你的帳號裡」（我草稿用的）
- **C. 連未加密也講**：完整帳號那句寫出「未加密」

我建議 **B**：對「身分證字號」這種東西，講「有加密」是使用者真的想知道的；
但帳號那句不提「未加密」（見上面的理由）。

### ② 要不要加「可展開的詳細說明」？

專案有一條鐵則：「懂了才不會誤會的概念，一律在網頁上就地白話解釋」。
資料放哪、誰看得到，算不算那種概念？

- **A. 只改句子**（最小改動）
- **B. 句子 ＋ 一個「詳細說明」連結**，點開講清楚「資料在哪、誰看得到、我們做了什麼保護」

我**沒有替你決定**——那條鐵則原文限定在「否則會把正常數字當成算錯」的概念，
資料放哪不完全屬於那一類。這是你的產品判斷。

### ③ ⚠️ 一句我建議現在**不要寫**的話 —— ✅ 條件已達成，見上面

> 這一節是提問當下（2026-07-29 上午）的狀態。**同日下午 C6 已跑過真 Supabase 並全過**，
> 條件解除，那句話可以寫了。保留原文是為了記住判準：**沒驗過的保證不寫**。

草稿裡我刻意**沒有**寫「別的使用者看不到你的資料」這種話，即使那是我們的設計目標。

原因：那件事**還沒被證明**。跨使用者隔離目前只有「模擬資料庫」的考題，
真正算數的驗收（拿 A 的登入憑證直接打資料庫讀 B 的資料）**要等 C6 對抗腳本跑過真的 Supabase**——
而那支腳本今天才剛修好兩個假綠。

**在那之前，文案只寫已經證明的部分**（要登入才進得來），不寫資料庫層的隔離。
等驗收過了再加，那時候是有憑有據的。

---

## 技術附註（給 Claude 自己，你不用看）

前端目前**完全不知道自己跑在哪個模式**（`grep hosted public/` 零命中），所以「依模式顯示不同句子」需要先牽一條線。兩個選項：

- **A. 後端加旗標**：`/api/settings` 回應多一個布林。⚠️ 必須**只在 HOSTED 送出**，否則會改到 LOCAL 的 API 回應形狀，撞到「LOCAL 零改動」契約。
- **B. 用網址判斷**：LOCAL 把 SPA 掛在 `/`、HOSTED 掛在 `/finance`，所以 `location.pathname` 現成可用、零後端改動。比較 hacky，但不會碰到契約。

實作時一併補考題：**九處文案在 HOSTED 模式下不得出現「只存」「這台電腦」「自動備份」**。

> 📌 進度（2026-08-06 #410 r6）：這條「補考題」目前只做到**「系統會替你留一份」那一族**——
> `test/vault-and-backup-integrity.test.js` 第二節的掃描題會走遍整個 `public/`，
> 要求每一句都自己交代模式或是否定句（觸發面已從三個字擴成「誰做的＋留了一份什麼」，
> 但仍是**文字比對**，換一組完全不同的詞就擋不住）。
> **「只存」「這台電腦」那一族還沒有**（第一類尚未定稿）。
> 也就是說：上面那句話還不是事實，是還沒做完的計畫——別把它當成已完成的保護。
