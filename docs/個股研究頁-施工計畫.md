# 個股研究頁施工計畫

> 狀態：**設計待 William 裁決，尚未動工**。本文件只規劃產品、資料與施工邊界，不修改 App。
>
> 角色分工（William 2026-07-24 更新）：Claude 主要實作 App、Codex 主要審核；Codex 在審核空檔可獨立製作本功能。審核永遠優先，本功能使用 Codex 獨立工作樹、獨立分支與獨立 PR，不與 Claude 共用未合併分支。
>
> 技術鐵則以 `AGENTS.md` 為單一真相；進度與產品裁決完成後才更新 `PROJECT.md`。

---

## 〇、先講結論

### 產品定位

投資組合頁回答「我的整體配置與風險如何」；個股研究頁回答「我為什麼持有這家公司、原先論點還成立嗎、下一次要檢查什麼」。

點擊投資組合裡的個股代號（例如 `AAPL`）後，建議在**新的瀏覽器分頁**開啟 App 內部頁面：

```text
http://localhost:4321/#stock?symbol=AAPL
```

這不是外部網站，也不另開第二套伺服器。原本的投資組合留在第一個分頁，研究頁在第二個分頁，方便來回對照。

### 技術方向

1. 沿用現有 `research` 資料，不另造第二套研究筆記。
2. 沿用 `portfolio-symbol.js` 的代號身分規則，不自行處理大小寫或空白。
3. 持股、成本、交易與研究筆記以本機資料庫為準。
4. v1 先做「人工判斷＋系統整理證據」，**不讓程式自動下買進／賣出結論**。
5. 外部基本面資料分階段加入；來源失敗時仍可閱讀與編輯自己的研究。
6. 先新增獨立檔案，最後才用短 PR 接上共同路由與持股表。

### Codex 推薦的施工方案

採用下方的 **方案 B：隔離建造＋短整合窗**。

- P1／P3 先以全新檔案完成大部分核心，Claude 幾乎不可能撞檔。
- 真的需要修改共同檔時，一次只占用一個短 PR。
- Claude 的修正若先合併，Codex 先更新基底再接線，不在 GitHub 上硬解衝突。
- 每一段都能單獨測試、單獨撤回。

---

## 一、現況盤點：已經有什麼

這個功能不是從零開始。現有 App 已有以下積木：

| 現成能力 | 位置 | 新頁怎麼沿用 |
|---|---|---|
| 個股研究資料 | `research` 集合 | 沿用同一筆研究，不另建重複筆記 |
| 投資論點／關鍵指標／風險 | `research.thesis / metrics / risks` | 直接搬進研究頁的核心區 |
| 檢查點 | `research.checkpoints` | 做成依日期排列的研究紀錄 |
| 研究顯示與表單 | `portfolio-research.js`、`portfolio-research-actions.js` | 抽出可共用的查找與表單規格，不複製 API 寫入流程 |
| 持股資料 | `holdings` | 股數、成本、現價、幣別與部位來源 |
| 個股層判準 | `holding.layer === 'stock'` | v1 只讓真正的個股進研究頁 |
| 代號身分 | `portfolio-symbol.js` | `aapl`、` AAPL `、`AAPL` 都視為同一家公司 |
| 成交紀錄 | `securityTrades` | 顯示該代號的買賣時間線，不重新計算來源金額 |
| 報價 | 現有 Yahoo chart 報價服務 | 顯示現價與更新時間，不拿來冒充官方財報 |
| 投資上限 | `portfolio-risk.js`＋設定 | 顯示個股占淨資產與 5% 軟上限，不另抄公式 |

### 不該重做的東西

- 不另建 `stockNotes` 或第二份 `research`。
- 不另寫一套 Yahoo 報價抓取。
- 不另寫代號正規化。
- 不在研究頁重算投資組合淨值、成本或 XIRR。
- 不讓研究頁直接修改 IBKR 持股或現金。

---

## 二、三種協作防撞方案

### 方案 A：完全排隊

Claude 的所有工作暫停或收官後，Codex 才從頭做到尾。

**優點**

- 衝突機率最低。
- 不需要兩邊頻繁更新基底。

**缺點**

- Codex 空檔無法利用。
- 小功能也必須等整條 Claude 主線結束。
- 審核與建造不能平行。

**適合情況**

- Claude 正在大改 `app.js`、`schema.js`、投資組合或全站 CSS。

### 方案 B：隔離建造＋短整合窗（推薦）

Codex 先在新檔案完成資料模型、評分器、畫面組裝與考題；接共同檔前先等 Claude 當下 PR 合併，再更新到最新 `main`，用短 PR 接線。

**優點**

- 大部分工作可與 Claude 平行。
- 共用檔修改量小，衝突好預測。
- 新功能每個零件有獨立契約與考題。
- Codex 接到審核任務時可以暫停建造，不影響 Claude。

**缺點**

- PR 數量較多。
- 最後仍有少數共同檔，必須安排「整合窗」。
- 每次共同檔 PR 前要確認 Claude 沒有同時修改。

**適合情況**

- 本功能有大量獨立新模組，只有路由、資料欄位與入口需要接現有系統。這正是本案的情況。

### 方案 C：另做一個獨立 HTML 小工具

建立另一張 HTML、另一套資料載入與樣式，完全不接現有 SPA 路由。

**優點**

- 初期幾乎不會撞 `app.js`。
- 可以很快做出原型。

**缺點**

- 會形成第二套路由、樣式、API 與互動規則。
- 容易和現有研究卡資料不同步。
- 長期維護成本最高，違反「單一真相」。
- 之後還是要重新整合回 App。

**結論**

不推薦。它只是把今天的 Git 衝突換成未來的架構衝突。

### 請 William 裁決 A

- [ ] A：完全排隊
- [ ] **B：隔離建造＋短整合窗（Codex 推薦）**
- [ ] C：另做獨立小工具

---

## 三、方案 B 的實際防撞規則

### 1. 工作樹與分支

| 人 | 工作位置 | 角色 |
|---|---|---|
| William | 主目錄、乾淨 `main` | 實際測試與合併 |
| Claude | Claude 隔離工作樹 | 主要 App 實作 |
| Codex | `榮祥森（投資理財）-codex` | 審核＋個股研究頁獨立分支 |

Codex 每段開工前：

1. `git fetch origin`
2. 由最新 `origin/main` 建立新的 `codex/...` 分支
3. 一段一 PR
4. PR 合併後刪分支，下一段再從新 `main` 出發

不把六段工程堆在一支長命分支上，避免基底愈來愈舊。

### 2. 新檔優先

Codex 可在 Claude 工作期間安全新增：

```text
public/modules/stock-research-model.js
public/modules/stock-research-score.js
public/modules/stock-research-view.js
public/modules/stock-research-page.js
public/stock-research.css
lib/services/stock-fundamentals.js       （未來）
test/stock-research-model.test.js
test/stock-research-score.test.js
test/stock-research-view.test.js
test/stock-research-page.test.js
test/stock-fundamentals.test.js          （未來）
```

每段新增自己的測試檔，不在 `server.test.js` 或其他大型共用測試檔尾端追加。

### 3. 共同檔預約表

以下檔案不是永遠不能動，而是「一次只能由一方持有」：

| 共同檔 | 為何需要 | Codex 何時才動 |
|---|---|---|
| `lib/schema.js` | 擴充研究欄位與深度驗證 | Claude 當下 schema PR 合併後，獨立 P2 |
| `lib/types.js` | 補研究資料型別 | 和 schema 同一支 P2 |
| `public/app.js` | 加入參數化研究頁路由 | 獨立 P4，先確認 Claude 沒改路由 |
| `public/index.html` | 載入研究頁專用 CSS（若採獨立 CSS） | 和路由同一支 P4 |
| `public/modules/portfolio-tables.js` | 個股代號變成入口 | 獨立 P5，最後才接 |
| `public/modules/portfolio-research.js` | 舊卡片增加「詳細研究」入口或共用查找 | 必要才動；能不動就不動 |
| `AGENTS.md` | 新模組邊界與同步點 | 功能落地的對應 PR 更新 |
| `PROJECT.md` | 進度與產品裁決 | 每段完成者更新；不要兩邊同時改 |

### 4. Claude 目前工作期間的禁止區

Claude 修證券交易或本輪複審問題時，Codex 不碰：

```text
lib/services/ib-sync.js
lib/services/securities-import.js
lib/routes/securities.js
public/modules/securities.js
public/modules/securities-view.js
test/securities-*.test.js
```

若 Claude 同時要動 `schema.js` 或 `app.js`，Codex 停在純新檔階段，等合併後再接。

### 5. 發現衝突時

1. 不在 GitHub 按鈕上硬解。
2. 不複製 Claude 的改動到 Codex 分支。
3. 等先到的 PR 合併。
4. Codex 更新到最新 `origin/main`，重新套用自己的小改動。
5. 三關與畫面回歸重跑。

### 6. 審核插隊

Claude 有 PR 要審時：

- Codex 先停個股研究頁。
- 已完成但未開 PR 的內容留在獨立分支，不碰 `main`。
- 審核完成後才繼續。
- 審核發現若牽涉本功能預定共用檔，先修主線，再更新施工計畫。

---

## 四、使用者流程

### 入口

1. 使用者在「投資組合」的持股表看到個股，例如 `AAPL`。
2. 個股代號顯示為可點擊文字；ETF、債券、黃金不變成入口。
3. 點擊後以新分頁打開 `#stock?symbol=AAPL`。
4. 新頁從本機資料組合：持股、研究筆記、交易紀錄、報價與投資上限。

### 頁面內操作

1. 先看到持有狀況與資料更新時間。
2. 閱讀「為什麼持有」與「什麼情況代表我看錯」。
3. 檢視五項評分及其理由。
4. 對照關鍵指標、估值情境、風險與催化劑。
5. 新增一筆有日期的檢查點。
6. 每次財報後更新評分與論點狀態。

### 不在 v1 做

- 自動推薦買進、賣出或目標張數。
- 直接替使用者下單。
- 研究頁修改持股、現金、成本或交易來源。
- AI 自動產生「看多／看空」結論。
- 社群評分、新聞情緒與網路文章大量爬取。
- 把不同公司硬套成完全相同的產業指標。

---

## 五、畫面藍圖

### 頁首

```text
AAPL  Apple Inc.                         資料更新：2026-07-24
個股研究與追蹤                         [更新資料] [編輯研究]
目前狀態：持有論點成立
```

- 代號與公司名稱是第一視覺訊號。
- 顯示資料來源與更新時間，避免把舊資料當現在。
- 提供「回投資組合」連結，但不取代瀏覽器上一頁。

### 第一層：現在持有多少

一列精簡數字，不做過大的英雄區：

| 項目 | 口徑 |
|---|---|
| 股數 | 同代號持股合併 |
| 成本 | 沿用投資組合模型 |
| 市值 | 沿用現價與匯率 |
| 未實現損益 | 沿用投資組合模型 |
| 占淨資產 | 沿用淨資產口徑 |
| 個股上限 | 沿用設定值，超過只提醒「凍結加碼」 |

### 第二層：研究結論

左右兩欄、不是卡片套卡片：

**投資論點**

- 我為什麼買？
- 我想驗證什麼？
- 目前看到哪些支持證據？

**反證條件**

- 發生什麼事代表論點破壞？
- 哪些風險只是價格波動，哪些是企業本質惡化？
- 下一次何時重新檢查？

### 第三層：五項評分

用水平長條或五列評分，不用雷達圖。雷達圖面積容易讓差異看起來比實際大，也較難精準比較。

| 構面 | 權重 | 要回答的問題 |
|---|---:|---|
| 商業品質 | 25% | 公司怎麼賺錢？護城河是否持續？ |
| 財務品質 | 25% | 成長、利潤、現金流與資產負債是否健康？ |
| 估值安全邊際 | 20% | 現價相對保守／基準／樂觀情境是否合理？ |
| 論點證據 | 20% | 最近資料支持還是否定原始論點？ |
| 風險控制 | 10% | 已辨認主要風險、部位是否符合上限？ |

總分只是整理工具，不是買賣訊號。頁面必須就地說明：

> 「高分不代表現在適合買；低分也不等於必須賣。分數的用途是讓你每次用同一把尺重新檢查。」

### 第四層：關鍵指標

- 顯示使用者自己選的 3–8 個指標。
- 每列包含：指標名稱、最新值、前期值、趨勢、資料期間、來源。
- 不同公司可以有不同指標，例如：
  - AAPL：服務營收成長、毛利率、iPhone 趨勢、回購。
  - GOOGL：搜尋廣告、Cloud 成長、營業利益率、資本支出。
- 缺資料顯示「尚未取得」，不可顯示 0。

### 第五層：估值情境

使用三情境表，不給單一神奇目標價：

| 情境 | 合理價值 | 主要假設 |
|---|---:|---|
| 保守 | 使用者填寫 | 成長放慢、估值收縮 |
| 基準 | 使用者填寫 | 目前論點大致成立 |
| 樂觀 | 使用者填寫 | 成長與利潤優於預期 |

- 顯示幣別與估值日期。
- 自動計算現價相對各情境的距離，但不顯示「應買／應賣」。
- v1 估值數字與假設由使用者填寫，避免未經校準的自動模型給出假精準答案。

### 第六層：風險、催化劑與檢查點

三個未巢狀的區塊：

- **風險**：已發生／持續觀察／解除。
- **催化劑**：可能讓論點被驗證的事件，不代表必然上漲。
- **檢查點**：日期、筆記、當時評分與是否改變論點。

### 第七層：我的交易紀錄

- 只讀顯示 `securityTrades` 中同代號資料。
- 保留來源原幣，不把 USD／TWD 混加。
- 欄位：成交日、買賣、數量、價格、費稅、淨應收付、來源。
- 這是查帳資料，不反推研究結論，也不改寫交易。

### 手機與桌面

- 桌面：內容主欄最大寬度約 1180–1240px，雙欄只用於可並讀的區塊。
- 手機：全部改單欄；分數、金額、長字不可溢出。
- 不使用巨大標題、不使用卡片套卡片、不讓圖表取代可讀的文字與表格。

---

## 六、評分方式

### 原則

1. 每項 0–5 分，只能選整數。
2. 每個分數必須附一句理由；沒有理由就視為「尚未評分」。
3. 總分只加總已完成項目；未完成時顯示「資料未完整」，不可把空白當 0。
4. 任何自動帶入的證據都要標來源與期間。
5. 評分歷史要留下，不能只保留最後一次。

### 建議量尺

| 分數 | 白話意思 |
|---:|---|
| 0 | 已有明確反證，原論點不成立 |
| 1 | 明顯偏弱，重要問題尚未改善 |
| 2 | 偏弱，仍有關鍵疑慮 |
| 3 | 普通／符合原先預期 |
| 4 | 穩健，證據多數支持 |
| 5 | 非常強，但仍不代表價格合理 |

### 加權

```text
總分
=（商業品質 ÷ 5）× 25
+（財務品質 ÷ 5）× 25
+（估值安全邊際 ÷ 5）× 20
+（論點證據 ÷ 5）× 20
+（風險控制 ÷ 5）× 10
```

畫面可轉成 0–100 分，但資料庫保留每項 0–5 原始分數與理由。

### 研究狀態

建議 enum：

```text
unreviewed      尚未評估
watching        觀察中
valid           持有論點成立
needs-review    待重新檢查
broken          論點已破壞
```

狀態由使用者選，程式不可僅因股價下跌或分數下降就自動改成 `broken`。

### 請 William 裁決 B

- [ ] **採五構面、0–5 分、加權成 100 分（Codex 推薦）**
- [ ] 不要總分，只保留五項獨立評分
- [ ] 第一版完全不評分，只做論點與檢查點

---

## 七、資料來源

### 第一級：本機財務真相（v1 必做）

| 資料 | 來源 | 地位 |
|---|---|---|
| 持股、成本、現價 | `holdings`＋現有投資組合模型 | 個人部位真相 |
| 淨資產與個股上限 | 現有 summary／risk 模組 | 風險口徑真相 |
| 買賣紀錄 | `securityTrades` | 查帳真相 |
| 論點、風險、檢查點、評分 | `research` | 使用者判斷真相 |
| 市場價格 | 現有 Yahoo chart 報價 | 目前價格參考 |

Yahoo 現階段只負責報價。它不是本功能的財報單一真相，也不把沒有正式契約的網頁欄位當長期資料介面。

### 第二級：美股官方財報（建議 v2）

美國 SEC 的 `data.sec.gov` 提供官方 EDGAR JSON API：

- Company Facts：`/api/xbrl/companyfacts/CIK##########.json`
- Submissions：公司申報歷史
- 不需要 API key。
- 必須由後端呼叫，使用可識別的 `User-Agent`，遵守 SEC fair-access 規範。
- 只支援有 SEC/XBRL 申報的公司；不同公司使用的 XBRL tag 可能不同，不能假設每檔資料形狀完全一致。

官方文件：

- https://www.sec.gov/search-filings/edgar-application-programming-interfaces
- https://www.sec.gov/about/webmaster-frequently-asked-questions

建議抓取：

- 營收、營業利益、淨利
- 營業現金流、資本支出、自由現金流（由明確公式衍生並標示）
- 現金、總負債
- 稀釋後 EPS、流通股數
- 申報期間與申報日期

### 第三級：台股官方資料（建議 v3）

- TWSE OpenAPI：https://openapi.twse.com.tw/
- 公開資訊觀測站：https://mops.twse.com.tw/mops/

台股與美股的會計欄位、申報期間、代號映射不同，應獨立做資料轉換器，不塞進美股解析器。

### 手動退路

外部 API 抓不到時：

- 自己的研究筆記、評分、估值與檢查點仍可使用。
- 顯示最後成功更新時間。
- 明確標「外部資料暫時無法更新」，不把舊資料偽裝成最新。
- 不因外部資料失敗而清空上次資料。

### 請 William 裁決 C

- [ ] **v1 只用本機資料＋現有報價；SEC 自動財報列 v2（Codex 推薦）**
- [ ] v1 就加入 SEC 自動財報
- [ ] 永遠只手動輸入，不接官方財報

---

## 八、資料庫欄位

### 1. 沿用 `research`

現有欄位保持：

```text
id
symbol
thesis
metrics
risks
checkpoints
```

建議新增：

```text
status
lastReviewedAt
scorecard
valuation
catalysts
thesisBreakers
watchMetrics
sources
scoreHistory
```

### 2. 建議形狀

```js
{
  id: "r1",
  symbol: "AAPL",
  thesis: "服務營收與生態系黏著度能支撐長期現金流",
  metrics: "服務營收成長、毛利率、回購",
  risks: "中國市場、AI 進度、估值",
  status: "valid",
  lastReviewedAt: "2026-07-24",

  scorecard: {
    business: 4,
    financial: 5,
    valuation: 2,
    evidence: 3,
    risk: 4,
    reasons: {
      business: "生態系黏著度仍高",
      financial: "現金流與回購穩健",
      valuation: "估值高於歷史中位",
      evidence: "服務成長支持，但 AI 尚待驗證",
      risk: "部位未超過個股上限"
    }
  },

  valuation: {
    currency: "USD",
    asOf: "2026-07-24",
    method: "情境估值",
    bear: 160,
    base: 205,
    bull: 250,
    assumptions: "由 William 手動填寫"
  },

  catalysts: [
    { id: "c1", text: "服務營收加速", horizon: "2026 H2", status: "watching" }
  ],
  thesisBreakers: [
    { id: "b1", text: "服務營收連續兩季低於預期", status: "watching" }
  ],
  watchMetrics: [
    { id: "m1", label: "服務營收成長率", unit: "%", value: 12, period: "2026 Q2", source: "SEC" }
  ],
  sources: [
    { id: "s1", label: "2026 Q2 10-Q", url: "https://...", asOf: "2026-07-24" }
  ],
  checkpoints: [
    { date: "2026-07-24", note: "財報後重看，論點維持" }
  ],
  scoreHistory: [
    { date: "2026-07-24", total: 73, scores: { business: 4, financial: 5, valuation: 2, evidence: 3, risk: 4 } }
  ]
}
```

### 3. 驗證要求

新增巢狀資料不可只標成泛用 `array`／`object` 就放行。需要：

- `status` 嚴格枚舉。
- 新增的完整日期走真日曆驗證。既有 `checkpoints[].date` 已有合法 `YYYY-MM` 舊資料，必須相容
  `YYYY-MM` 或 `YYYY-MM-DD`；不可因新增深度驗證而把現有研究卡誤擋。若未來要統一完整日期，另做有備份的明確搬家。
- 分數只接受 0–5 的有限數字。
- 估值金額只接受有限數字，幣別走既有 `CURRENCIES`。
- 每個陣列元素必須是物件，文字欄必須是字串。
- URL 只存字串，顯示時仍要 `esc()`；連結限制 `https:`／`http:`，不可接受 `javascript:`。
- `symbol` 繼續走 `normalizePortfolioSymbol()`。
- 一個代號只保留一筆研究；新增前要先查既有代號，避免 `AAPL` 重複。
- 寫入前後都要防原型鍵；若未使用文字當 map key，也不能因此省略輸入清理。

### 4. 外部財報快取（v2 才加）

不把官方財報混進使用者手寫的 `research`。建議另建**服務層擁有、前端唯讀**的集合：

```text
stockFundamentals
```

最小欄位：

```text
symbol
market
source
sourceId        （例如 CIK）
fetchedAt
periodEnd
currency
facts
```

- 不進通用 CRUD 白名單。
- 只由基本面抓取服務寫。
- 使用者匯出備份時保留；機密投影無額外敏感欄位。
- 外部刷新採 await 後重讀再合併，禁止拿網路前舊整包 DB 寫回。
- 抓取失敗保留舊快取，不寫空資料覆蓋。

---

## 九、路由與開新分頁

### 推薦

```text
#stock?symbol=AAPL
```

理由：

- 只需讓 router 認出 `stock`，參數用 `URLSearchParams`。
- 不必把每個代號加進 `ROUTES`。
- 重新整理仍能回到同一家公司。
- 可安全使用 `encodeURIComponent(symbol)`。
- 不在側欄新增「個股研究」主入口，避免沒有代號時進到空頁。

持股表個股代號：

```html
<a href="#stock?symbol=AAPL" target="_blank" rel="noopener">AAPL</a>
```

只對 `layer === 'stock'` 生效。其他層維持普通文字。

### 邊界

- 沒有 `symbol`：顯示「請從投資組合點選一檔個股」。
- 找不到持股、但有研究：仍可開啟，標示「目前未持有」。
- 找不到持股也沒有研究：顯示空狀態，不自動建立資料。
- ETF 若被錯設成個股層：頁面可以開，但顯示資料取決於來源；不在路由層猜它是不是公司。
- 所有 async render 在寫 DOM 前走 route sequence／generation guard。

### 請 William 裁決 D

- [ ] **新瀏覽器分頁開 App 內部研究頁（Codex 推薦）**
- [ ] 同一分頁切換，保留側欄
- [ ] 彈跳視窗（不推薦；內容太多且不利深度研究）

---

## 十、分階段 PR

每支 PR 都要：`npm run typecheck`、`npm run lint`、`npm test` 全綠；只用合成資料，不讀真實 `store.db/store.json`。前端整合段另做桌面／手機瀏覽器回歸。

### D0：施工計畫（本文件）

**只新增**

```text
docs/個股研究頁-施工計畫.md
```

不改程式、不需重啟。由 William 裁決 A–F 後才進 P1。

### P1：個股研究純模型＋評分器

**新檔**

```text
public/modules/stock-research-model.js
public/modules/stock-research-score.js
test/stock-research-model.test.js
test/stock-research-score.test.js
```

內容：

- 組合同代號持股摘要。
- 從 research 找正確代號。
- 五構面加權、缺值處理、評分理由完整度。
- 研究狀態與顯示文字。
- 不碰 DOM、API、資料庫與共同檔。

**可與 Claude 完全平行。**

### P2：研究欄位與資料驗證

**共同檔短整合窗**

```text
lib/schema.js
lib/types.js
```

**新測試**

```text
test/stock-research-schema.test.js
```

內容：

- 擴充現有 `research`，不新建重複筆記集合。
- 深度驗證 scorecard／valuation／陣列元素。
- 通用 CRUD 仍使用 `/api/research`。
- 舊研究資料沒有新欄位時保持合法，不需搬家。
- 壞欄位在 CRUD 明確 400；備份匯入不得靜默吃掉整份研究。

**開工條件**：Claude 當下涉及 `schema.js/types.js` 的 PR 已合併。

### P3：純畫面組裝

**新檔**

```text
public/modules/stock-research-view.js
public/stock-research.css
test/stock-research-view.test.js
```

內容：

- 頁首、持股摘要、論點／反證、評分、估值、風險、催化劑、檢查點、交易表。
- 所有使用者文字 `esc()`。
- 交易依原幣呈現，不跨幣相加。
- 空資料、舊資料、極長文字與手機寬度考題。
- 此階段只產 HTML／view model，不碰路由。

**可與 Claude 完全平行。**

### P4：頁面控制器與路由

**新檔**

```text
public/modules/stock-research-page.js
test/stock-research-page.test.js
```

**共同檔短整合窗**

```text
public/app.js
public/index.html            （載入獨立 CSS；若改用 styles.css 則二選一）
```

內容：

- 解析 `#stock?symbol=...`。
- 平行讀 holdings／research／securityTrades／summary／settings。
- route sequence＋page generation 防舊請求蓋新頁。
- 編輯研究與新增檢查點沿用同一 `/research` 寫入。
- 開研究頁不刷新 IBKR、不寫持股。

**開工條件**：Claude 沒有持有 `app.js/index.html`。

### P5：投資組合入口

**共同檔短整合窗**

```text
public/modules/portfolio-tables.js
AGENTS.md
PROJECT.md
```

**新測試**

```text
test/stock-research-entry.test.js
```

內容：

- 只有個股層代號變成新分頁連結。
- 使用 `encodeURIComponent`、`target="_blank"`、`rel="noopener"`。
- 名稱、ETF、債券與其他按鈕不受影響。
- 更新 AGENTS 模組邊界、同步點與 PROJECT 進度。

**這是 v1 收官 PR。**

### P6：官方基本面資料（v2，另行裁決）

**新檔為主**

```text
lib/services/stock-fundamentals.js
lib/routes/stock-fundamentals.js
test/stock-fundamentals.test.js
```

**可能共同檔**

```text
lib/schema.js
lib/types.js
lib/store.js
server.js 或路由掛載處
```

內容：

- SEC CIK 映射與 Company Facts。
- 後端 User-Agent、timeout、快取、失敗保留舊值。
- 指標附期間、幣別、來源與原始 fact 名。
- 不同公司 tag 不一致時顯示缺資料，不猜數字。
- 台股資料另開 P7，不與美股解析混在一支 PR。

---

## 十一、驗收考題

### 身分與入口

- `aapl`、` AAPL `、`AAPL` 都開同一頁。
- 同代號分散兩筆持股，摘要只顯示一家公司並合併股數／成本。
- 非個股層代號不是連結。
- 惡意代號不能插入 HTML 或 `javascript:`。

### 數字

- 持股成本、市值與損益和投資組合頁相同。
- 個股占比使用淨資產分母，和紀律檢查相同。
- 個股上限明確設定 0 時顯示 0，不退回預設 5。
- USD／TWD 交易不跨幣相加。
- 缺值顯示「尚無資料」，不顯示 0。

### 評分

- 0 分是合法值，不可因 falsy 被當成未填。
- 6、負數、NaN、Infinity、字串分數被擋。
- 缺理由的分數顯示未完成。
- 總分權重正確，未評分項不偷偷以 0 拉低。
- 顯示文字不把分數翻成自動買賣建議。

### 寫入與舊資料

- 現有只有 thesis／metrics／risks 的研究卡可正常開啟。
- 現有 `checkpoints[].date` 為 `YYYY-MM` 的研究卡仍可讀寫，不因新驗證被濾除或炸存檔。
- 儲存新欄位不會清掉舊 checkpoints。
- 只改評分不會覆蓋網路等待期間新增的檢查點。
- 壞巢狀陣列在 CRUD 400；備份還原 fail-closed。
- 新頁不會寫 holdings、accounts、securityTrades。

### 非同步與畫面

- 快速由 AAPL 切 GOOGL，慢回來的 AAPL 不蓋掉 GOOGL。
- 研究儲存完成時若頁面已切走，不重畫舊頁。
- 外部資料失敗時自己的研究仍可讀寫。
- 桌面 1280px、平板 768px、手機 390px 無重疊與橫向溢出。
- 新分頁可重新整理、可複製網址、可直接回到相同代號。

---

## 十二、風險與對策

| 風險 | 對策 |
|---|---|
| Claude 與 Codex 同改 schema/app/styles | 新檔先做；共同檔分成短 PR，開工前預約 |
| 第二套研究資料漂移 | 沿用 `research`，不新建重複筆記 |
| 自動評分造成假精準 | v1 由使用者評分，系統只整理證據與加權 |
| Yahoo 欄位不穩 | Yahoo 只沿用現有報價；財報使用官方來源 |
| SEC tag 不一致 | 每個指標做候選 tag 與來源標示；抓不到不猜 |
| 網路失敗清空舊資料 | 失敗保留快取與手寫研究 |
| 研究頁數字與投組不同 | 引用現有模型／格式器，不另抄公式 |
| 點連結造成原頁消失 | 新分頁＋`noopener` |
| 巢狀資料讓頁面崩潰 | 深度 schema、strip/throw/匯入三條路都考 |
| 功能無止境膨脹 | v1 收在 P1–P5；官方基本面另行裁決 |

---

## 十三、需要 William 最終裁決

### E：第一版支援哪些對象

- [ ] **只有目前持有、且層＝個股的公司（Codex 推薦）**
- [ ] 持有個股＋願望清單個股
- [ ] 任意輸入代號都可建立研究

推薦理由：先讓資料身分最清楚、使用頻率最高的持有個股跑順；願望清單與任意代號之後再擴，不讓 v1 同時處理「持有／未持有」兩套語意。

### F：舊的個股研究卡

- [ ] **保留在投資組合頁做摘要，新增「詳細研究」入口（Codex 推薦）**
- [ ] 從投資組合移除，只留新研究頁
- [ ] 保留原樣，不加入口；只有持股表代號可進

推薦理由：摘要卡適合快速掃描，新頁適合深入思考，兩者用途不同；但兩邊必須讀同一筆 `research`。

### 裁決清單

請 William 回覆：

```text
A：協作方案
B：評分方式
C：資料來源階段
D：開頁方式
E：v1 對象
F：舊研究卡
```

Codex 收到六項裁決後：

1. 把決定寫回本文件。
2. 先開 P1（純模型＋評分器，不碰 Claude 工作檔）。
3. 每段 PR 合併後才開下一段。
4. Claude 有審核需求時暫停施工，先完成審核。
