# 帳單解析器通用化（AI 解析）施工計畫

> **活文件**：建立 2026-08-10（William 提出：「以後要開放給其他人使用，不可能先給範本解析」）。
> 慣例同 AGENTS.md：一步一 PR、三道關全綠、互審後合併。
> ⚠️ **裁決機制：每一期動工前，該期所屬的裁決點（§七）要先拍板。** 現況（2026-08-15）：★1/★2/★4/★6 主項、★5 與 ★1 子項「LOCAL 同意粒度」均已拍板；P0 系列全收、**P1 已拆成 P1a／P1b 開工（William 2026-08-12 拍板順序＝P1a 先）**——P1a（銀行身分維度）不涉裁決點、已施工；★3 已拍板（2026-08-12＝Anthropic）＝P1b 解鎖、P1b-1（後端引擎）已施工。**★4 子項已全數拍板（2026-08-14/15）＝P2 解鎖、P2-1（配方純模組）已施工**。逐項見 §七。

## 一、為什麼要做（病根一句話）

今天的解析器是「**手寫翻譯稿**」模式：信用卡有台新、富邦兩份（`lib/statement.js`），銀行對帳單只有台新綜合對帳單一份（`lib/bank-statement.js`）。**每多支援一家銀行＝等工程師手寫一份新翻譯稿。** 開放多人使用後，使用者上傳國泰、玉山、中信、郵局……不可能一家一家先收範本。需要一條「**沒看過的帳單也解得了**」的路。

（附註：與「電子發票即時預算」願景不衝突——那是即時消費的資料線，本計畫是帳單這條線，兩者互補。）

## 二、核心設計＝三件組（會計師・答案卷・規則本）

1. **AI 解析＝請一位看得懂任何帳單的會計師。**
   上傳的 PDF 在 app 伺服器的**記憶體內**解密與抽字（原始檔不落地、不進版控——機密流向的完整現況見 §三），抽出的文字交給 AI 模型，照一張**固定的答案格式**填：日期、說明、金額、方向、幣別、帳戶、逐筆餘額。AI 的強項正是「沒看過的版面也讀得懂」——所以不需要預先範本。

2. **數學對帳閘＝帳單自帶答案卷。**
   AI 解完，程式**自己算一遍**：帳軋得平才收；軋不平就整份擋下、標給使用者人工確認。**絕不靜靜吞下一份對不起來的帳單。**
   ⚠️ **誠實劃界（審查 r1 補正）**：對帳閘擋的是「**不一致**」，擋不住「**自洽的錯**」——若 AI 把一整組數字抄錯得剛好互相吻合（或整段漏抄後仍自洽），數學會照樣軋平。對付自洽錯靠五道互補防線（P2-2a 起）：①強閘的**逐筆餘額連續**（每一筆要跟上一筆的餘額咬合，自洽地抄錯整組的難度大增）②**控制總額互驗**——AI 從「合計」列抄回來的值 vs 它自己逐筆相加（⚠️ 主詞：管線只知道 AI 交回什麼，證明不了帳單印的是什麼——#490 r7/r8）（⚠️ 兩者是**不同來源欄位、但同一條 AI 路徑抽出**，並非獨立解析器——連合計一起抄成自洽的錯仍可能通過；P2-2a 起由引擎機械交叉驗證＝totals）③**接地檢查**（P2-2a）——答案卷每個金額都要在帳單原文逐字找得到，自洽地「發明」一組數字藏不住（殘洞誠實記載、不宣稱窮盡）④既有的**預覽→人工確認**流程 ⑤抽樣比對帳單原文。承諾照實寫：**擋得住「造成不一致」的錯；自洽錯有防線壓低**。⚠️ **攔截率已於 P1b-3 實測（2026-08-13）——數字與誠實劃界見 §八**；量到的是**條件攔截率**（閘的性質），不是 AI 的錯誤率。

3. **配方快取＝會計師第一次看完就寫下翻譯規則。**
   第一次遇到某銀行的版式，讓 AI 順手產生一份「**配方**」（版面錨點與**欄位角色文字＋列型枚舉**的**宣告式規則檔**——只是資料、不是可執行程式，見 ★4；⚠️ 座標分欄與 running 餘額判向等幾何機關**全在引擎、不在配方**（格式 A 拍板）——P2-2/P2-3 勿把幾何資料塞回配方）存起來；之後同版式的帳單直接用配方**在 app 內解**——快、免費、內文不再送 AI。配方失靈（銀行改版）→ **造成不一致的失靈**對帳閘當場抓到（⚠️ 同 §二誠實劃界：自洽的誤解析、以及落在首筆/餘額缺席盲區的列，閘抓不到——引擎另以列判準收緊壓低，r4）→ 依 ★1 拍板的同意粒度決定是否重新送 AI 重生配方。

## 三、機密流向的誠實現況（★1 隱私裁決的前提）

⚠️ 本節是**現況盤點**，審查 r1 指出第一版把它寫反了，以下照 repo 與契約（`docs/contracts/cloud-security.md`）逐項核對過：

1. **原始 PDF**：在 app 伺服器記憶體內解析、不持久化（現況如此，本計畫維持）。
2. **帳單密碼（P0.5 後統一）**：解析當下只在記憶體使用；**預設用完即丟，只有使用者勾「記住」才持久化**——記住的密碼待遇比照信用卡卡密（LOCAL 明文存本機 db、**LOCAL 匯出仍完整含密碼——備份檔本身是敏感檔**；HOSTED 加密儲存、**只有 HOSTED 匯出才剝除**、機密投影不進瀏覽器）。信用卡卡密仍另存在卡片資料裡（每月同組、便於自動試解）；記住的帳單密碼另存在 `settings.rememberedStatementPasswords`（單一 JSON 字串機密）。完整同步點見收支契約「匯入密碼池」節。
3. **兩道傳輸邊界要分開講**：①瀏覽器 → app 伺服器（LOCAL 版＝同一台電腦；**HOSTED 版＝帳單與密碼已經送到營運方的伺服器**——這是今天就存在的邊界，不是本計畫新增的）②app 伺服器 → AI 供應商（**本計畫新增的那一道**，★1 要裁的就是它）。
4. **log 與快取**：密碼與帳單內文不落 log、不進快取（施工時列為考題）。

「哪裡動、哪裡不動」的真實接縫（r1 補正——第一版寫「下游完全不動」過頭了）：

- **動**：解析引擎（`parseStatement`／`parseBankStatement` 內部加 AI 路線）；**配方儲存**（新 schema 欄位＋repo 櫃檯，★4 若共用還涉及跨租戶儲存設計）；**銀行身分維度**（支援第二家銀行起，帳戶配對 `matchAccount`、去重鍵 `bankRef`、新帳戶命名——現行寫死「台新」——都要加機構維度，否則不同銀行的相同可見帳號段會互相誤配）。
- **不動（產品層流程）**：預覽→確認、分箱規則（內轉／繳卡費／劃撥…）、分類自動學習、店名規則、帳戶餘額更新的使用者體驗。
- 沿用既有鐵則：真實帳單絕不進版控；測試帳號末碼一律明顯假值。

## 四、對帳閘的三個等級（誠實分級）

| 級別 | 判準 | 常見於 | 真正擋得住的 |
|---|---|---|---|
| **強閘** | 逐筆餘額連續（上筆餘額 ± 本筆 ＝ 下筆餘額）＋期初 + Σ進出 ＝ 期末（⚠️「期初＋Σ＝期末」**P0 未實作**——期初餘額尚未抽取，**每帳戶第一筆的金額/方向驗不到**（P0 r1#3 坐實）；verdict 誠實計數 `firstRowsUnverified`、考題釘住界線，期初抽取待真帳單校準後補） | 銀行對帳單（台新有逐筆餘額） | 漏抄／多抄／金額錯／方向錯——**當它們造成不一致時**（連餘額一起抄成自洽的錯仍可能通過，見 §二-2；首筆見左欄界線） |
| **中閘** | 總額對帳：本期應繳 ＝ 前期 − 繳款 + 新增消費 ± 退款/利息/費用等調整列 | 信用卡帳單 | 合計層級的不一致（單筆互抵的錯攔不到）。⚠️ P0 之前的解析器沒有輸出前期餘額/繳款欄；✅ **P0 已補**＝`extractStatementTotals` 四格（與應繳金額共用同一副序數對位機關）。⚠️ P0 只有「摘要等式」是**擋下型**；「明細對摘要總額」＝**影子檢查不擋**（P0 r1#1 誤擋風險→P0.2 公式已依真郵寄版校準；**維持影子＝William 裁示**，⏰ 絆線＝分期/年費/循環息月份出現且影子仍安靜時提請裁決升級為擋——兩處 advisories.push 改 problems＋考題同步、一起動） |
| **弱閘** | 只有筆數／結構檢核（日期遞增、金額是數字） | 少數格式 | 結構壞掉而已——**這其實是標籤不是閘**，能否匯入見 ★6 |

閘級別跟著匯入預覽顯示，就地白話解釋（「這份我幫你驗到什麼程度」）。（P0 先做後端：擋下即刻生效、預覽回應帶 `reconcile` 級別；✅ **前端就地顯示已接**（P0 前端子項）＝兩個預覽窗共用 `gateSummaryHtml` 白話翻譯——通過講「驗了幾關」、弱級老實說「沒有可驗算的數字」、影子提醒原文列出不擋。）

## 五、分期施工（每期＝一支或數支小 PR）

- **P0 對帳閘先行（不碰 AI，立刻有價值）**：三級閘獨立模組，先套在現有台新/富邦解析器上（現有翻譯稿從此被守著）；**含補齊中閘所需欄位**（前期餘額、繳款額的抽取與輸出）。考題＝合成帳單＋突變——**每級只宣稱該級真正看得到的錯**（強閘：改壞一筆金額→紅；中閘：改壞合計→紅；弱閘：只驗結構壞掉→紅）。
- **P0.5 匯入密碼流程統一（✅ William 2026-08-11 拍板；✅ 同日落地——完整同步點見收支契約「匯入密碼池」節）**：銀行與信用卡帳單的匯入流程一致化——新帳單跳出密碼輸入＋「是否記住這組密碼」勾選（預設不勾）；記住後下次匯入**自動逐一嘗試所有已存密碼**（比照信用卡既有的「逐一試各卡密碼」模式，銀行納入同一機制）；都試不開才再請使用者輸入。⚠️ 這把銀行密碼從「單次即丟」改為「**可選儲存**」——儲存待遇完全比照卡密（LOCAL 明文／HOSTED 加密／HOSTED 匯出剝除／機密投影不進瀏覽器）。
- **P1 AI 解析引擎＋銀行身分維度（2026-08-12 拆兩支，William 拍板順序＝a 先）**：
  - **P1a 銀行身分維度（✅ 2026-08-12 落地，本支）**：帳戶配對／去重鍵／新帳戶命名加上機構維度（多銀行的前提工程；不碰 AI、不需鑰匙）。落地要點＝帳戶 `bank` 欄（新建蓋戳、比對不回填、無戳祖父條款）＋去重鍵雙格式（台新位元組級凍結、他行 `bank2|機構|…`）＋顯示層 812- 補碼只屬台新；完整同步點見收支契約「帳戶完整帳號與餘額匯入」「帳單多銀行與多格式解析」節。
  - **P1b AI 解析引擎（★3 已拍板＝Anthropic；2026-08-12 拆三小支）**：
    - **P1b-1 後端引擎（✅ 2026-08-12 落地，本支）**：抽字→AI（結構化輸出答案卷）→fail-closed 驗收→過閘→進既有預覽；四道規矩（HOSTED 停止線寫死／useAi AI 要求旗標（舊名「同意旗標」；2026-08-13 起確認窗僅 aiAskBeforeSend 開啟時出現）——前端蓋好前後端這條路不通／settings.aiApiKey 機密欄／★6 AI 必須過強閘）＋模型階梯（時為 Haiku→閘紅升 Sonnet 一次；**裁示⑥ 2026-08-15 改版＝Sonnet→Opus**、P2-2a 落地）；完整同步點見收支契約「AI 解析路線 P1b」節。考題全假引擎＝零鑰匙零費用。
    - **P1b-2 前端（✅ 2026-08-12 落地，本支）**：上傳認不得（後端新增 `code:'bank_unrecognized'` 判準；對帳閘紅刻意無 code＝走不到 AI）→ 同意確認窗（顯示送哪份/去向/費用級距，按同意才帶 `useAi`；**每次都問、不記住**（⚠️ 2026-08-13 翻案＝`aiAskBeforeSend` 控制、**預設不問直接送**；#455））→ 預覽窗徽章誠實揭露「AI 讀的、驗不到機構名/帳號/摘要」→ 套用憑票（`aiTicket` 只活在單次預覽閉包）；設定頁「AI 解析鑰匙」卡（不回顯、清除入口由 `aiApiKeySet` 把關、四顆就地解釋 ⓘ）。完整同步點見收支契約「AI 解析路線 P1b」節的前端落點段。
    - **P1b-3 攔截率實測（✅ 2026-08-13 落地，本支）**：**故障注入** 23 型 AI 誤讀、逐型量閘攔不攔得住，結果與誠實劃界寫進 §八（考題 `test/ai-gate-interception.test.js` 與 §八 的數字**互扣**，改一邊另一邊就紅）。⚠️ 量的是**條件攔截率**（閘的性質），不是 AI 的錯誤率——後者要真 AI ＋標準答案語料，未做。真帳單只做本機煙霧測。
    ⚠️ **停止線：HOSTED 在 ★1 的同意機制完成前，AI 路線不得啟用（P1b-1 已寫死＋考題）。**
- **P2 配方快取**：AI 產配方（宣告式規則檔）→ 存 db（新 schema＋repo 櫃檯）→ 同版式 app 內解 → 閘守著 → 失靈依 ★1 同意粒度回退。突變考題：改壞配方→閘紅→回退路徑真的動起來。**拆三小支（2026-08-15 開工）**：
    - **P2-1 配方純模組（✅ 2026-08-15 落地，本支）**：格式 A 定義（填格子＝字面文字＋枚舉、**配方裡零正則**）＋零帳單內容機械驗證 `validateRecipeStrict`（限長/禁 4 位以上數字（NFKC＋squash 後數總量）/禁 `*` 與 `|`/鍵白名單、鍵名不回聲）＋泛化引擎 `parseWithRecipe`（bank-statement.js 幾何機關同源泛化，輸出同形狀 ParsedBankFull；台新等價考題釘同源性）＋**出生把關三關**（零內容 `validateRecipeStrict`＋對照出生帳單 `validateRecipeAgainstStatement`（錨點不得等於交易內文、不得命中交易列——字元檢查擋不住無數字的收款人文字，r1#2）＋`recipeReproduces` 逐欄重現使用者確認的 AI 答案，任一關不過＝不存）。完整同步點見收支契約「配方快取 P2」節。
    - **P2-2 儲存與讀取接線（✅ 2026-08-15 落地，本支）**：`parseRecipes` 唯讀集合＋schema/emptyDb/types 登記（寫入櫃檯與生成一起在 P2-3）；認不得後先試配方（零元零外送、**不需 useAi**、pdf_password 不落配方）→ current 失靈自動退 previous（裁示④細部）→ 全敗才輪 AI；配方閘比照 AI（同一顆 assertAiBankReconciled）；apply 原子交易內計數＝畢業（連 5）/回滾互換（previous 升 current、streak 重數）/疑似過期（經 AI 確認票、完成匯入那一刻標）。完整同步點見收支契約「配方快取 P2」節的 P2-2 段。
    - **P2-2a AI 路線強化（✅ 2026-08-15 落地，本支）**：裁示⑥ 模型配置（`AI_BANK_MODELS`＝Sonnet 預設／Opus 升級、Haiku 退出）＋裁示⑧ 兩道：**接地檢查** `assertAiBankGrounded`（答案卷每個金額須在帳單原文逐字找得到＝自洽錯主力剋星、零 AI 成本；NFKC＋日期剔除＋配對拼接、殘洞誠實記載；不接地＝ai_bad_answer 走階梯）＋**合計欄進答案卷**（`totals`{筆數/支出合計/存入合計}、欄位必填、null＝**AI 沒交回那一欄**誠實缺席（碼名 `not-read`，#490 r7）；`assertAiBankReconciled` 交叉驗證（BAL_EPS 同尺）＝ai_totals_mismatch、混幣整道跳過、兼補「每帳戶第一筆未驗」盲區）。另併：傳輸層 max_tokens 16000＋effort medium（Sonnet 5/Opus 5 預設 adaptive thinking 與答案共搶上限；殘餘＝medium 起點、真帳單校準再裁 escalation 是否 high）。
    - **P2-3 AI 產配方（✅ 2026-08-16 落地，本支）**：AI 票路線**兌票寫入成功後**另加至多一發生成呼叫（apply 零解析重跑；P2-4 雙讀＋仲裁走滿時它是本次上傳第四發。黃金樣本＝使用者確認的那份；一律 Opus＝裁示⑥）填配方格子（RECIPE_SCHEMA＋白名單化）→ **出生三關**（strict＋against-statement＋reproduces、各自 reason code，r2#5）→ repo 櫃檯存檔/重生（rebirths 接線＝內建化候選訊號；A6「份」判準＝imported>0 明文）；前端徽章「版面規則卡讀的」＋完成訊息一句。失敗不連坐匯入。**P2 三支全數收官**。
    - **裁示⑦ 新版式雙讀——整合流程（✅ 2026-08-16 落地＝P2-4 本支；拍板兩點＝仲裁與預設值同日 William 裁）**：①觸發＝「模板認不得＋無配方命中」的**首次** AI 解析（配方畢業後不再發生）②流程＝Sonnet 與 Opus **各自獨立**解一次（兩發並行）→程式逐欄比對「會影響帳本與去重鍵」的欄位（金額/方向/日期/帳號末碼/幣別/摘要）→**全一致**＝走既有強閘→預覽（徽章加註「雙讀一致」）③**不一致＝送 Fable（`claude-fable-5`）第三讀仲裁**（拍板取代原草案 a 的「給不給逃生門」二選一；Fable **獨立重解、不看前兩份答案**＝「送給 Fable 讀看看」的拍板語意、也避免錨定）：Fable 的答案與兩份既有答案**用同一組欄位、全欄一致才算同一份**逐欄比對——與其中**恰一份**全一致＝採用那份（照樣過強閘＋確認票；預覽徽章標「三讀仲裁」）；三份互不相同（含 Fable 只部分吻合＝不算一致）＝**預覽標紅不一致欄位＋只給手動記帳出口**（標紅 UX 承襲原草案、Grok G2 補回）。**沒有「使用者自己挑一份」的逃生門**（使用者無從判斷誰對、選錯＝錯的金額進帳本且配方會從錯答案學）④與升級階梯的關係（**草案設計、非本次拍板項**——本次只拍 a/b 兩點，Grok G1）＝雙讀已含 Opus，雙讀開啟時**取代**階梯（省一發重試、換兩發首解）；**開關關閉＝回到單讀＋既有升級階梯**⑤成本＝一致路徑約 NT$6–9/新版式一次性；仲裁路徑另加一發 Fable（**成本句已連動（P2-4 落地）**：「至多 4 發」＝雙讀 2＋仲裁 1＋生成 1、關雙讀＝至多 3——server.js／契約／hosted-auth／本檔 P2-3 段全改畢）⑥開關＝settings 新欄、**預設開（拍板 b）**；HOSTED 停止線不變。
- **P3 多人前置（併多人上線階段）**：隱私同意畫面、成本護欄、掃描圖檔 OCR（另案）。✅ **成本護欄切片已提前落地（C1，2026-08-26，William 拍板）**＝發數上限制：單張預設 6 發／單日預設 20 發（設定頁可調、超限擋下白話說明；擋點＝ai-transport 每發先過 lib/ai-budget.js）——⚠️ 用**發數**不用金額：實際 NT$ 要等 Anthropic 帳單實測（§六絆線），金額上限講不誠實。隱私同意畫面與 OCR 仍待 P3。

## 六、成本粗估（有計算基礎的級距，正式數字待 ★3 實測）

計算基礎＝2026-08 公版定價（每百萬 token：Haiku 級 $1 輸入／$5 輸出；Sonnet 級 $3／$15）、一份帳單抽字後約 5,000–10,000 token 輸入＋1,000–3,000 token 輸出：**首次解析一份 ≈ 台幣 0.5–3 元**；批次（不趕時間的回顧匯入）可再半價；配方生成一次性 ≈ 台幣 2–6 元／版式。有配方後的日常解析 ≈ 0（不呼叫 AI）。
⚠️ 這是**級距直覺**，不是報價：改版重生、回退重試、驗證失敗重跑都會增加呼叫；**★3 選定供應商後，用合成語料實測輸入輸出量與回退率，才給正式數字**。

## 七、★ 裁決點（拍板前不動工）

- **★1 隱私路線（最大的一個；前提＝§三的誠實現況）**：
  (a) 雲端 AI＋明確同意；(b) 全本機小模型（不出門，代價＝準確度與安裝門檻）；(c) 折衷＝平常 app 內配方解，**只有新版式上雲一次**。
  ✅ **主路線已拍板（William 2026-08-11）＝(c) 折衷**。子項現況：**LOCAL 同意粒度 ✅ 已拍板（William 2026-08-12）＝每次要把帳單內文送 AI 之前先跳確認窗**（顯示送出哪份、去向、估計費用，按了才送；⚠️ 2026-08-13 已翻案＝預設不問直接送、`aiAskBeforeSend` 打開才問；不記住同意——想省點擊 P2 再議）；**HOSTED 同意機制**＝P3 多人前置的隱私同意畫面，未動工前 AI 路線在 HOSTED 寫死不啟用（§五停止線）；**供應商留存條件**＝選 ★3 時的必要條件（比較功課的必查欄，見 ★3）。
- **★2 先做哪類**：✅ **已拍板（William 2026-08-11）＝銀行對帳單先**（強閘、現金流真相）。
- **★3 AI 用誰的鑰匙（API key）**：你自用＝你的 key；開放多人＝營運方 key（環境機密、非租戶資料）。若未來允許使用者自帶 key＝新增一個要加密儲存、投影剝除、可清除的機密欄位（比照 flexToken 待遇）。含供應商與模型選擇（判準：結構化輸出穩、長文件準；成本靠配方架構省，不靠挑牌子）。✅ **已拍板（William 2026-08-12）＝Anthropic＋自己的 key**：四欄比較（留存/訓練政策、結構化輸出、長文件、價格）交裁後選定——三家留存同級（付費 API 不訓練＋短期刪除）、價差非決定因素（配方後趨近零）、決定因素＝生態同家工程風險最低（已誠實揭露自家人成分；供應商做成可抽換引擎接縫＝拍板可逆）。~~起步 Haiku、閘紅升 Sonnet~~（**裁示⑥ 2026-08-15 改版**＝解析預設 Sonnet、閘紅升 Opus 重試一次、Haiku 退出解析路徑；**寫配方一律 Opus**（配方錯誤會被免費複製到未來每一期、落點 P2-3）；**裁示⑦ 新版式雙讀＝開、只限新版式首解**（Sonnet＋Opus 各解一次、程式逐欄比對會影響帳本與去重鍵的欄位；**整合流程已定稿（William 2026-08-16 拍板兩點，詳 §五）＝不一致先送 Fable 第三讀仲裁、三份仍無全欄共識才標紅轉手動＋開關預設開**；升級階梯取代關係仍是草案項）——約 NT$6–9/新版式一次性、配方畢業後不再發生）；鑰匙＝William 自辦（platform.claude.com、與 Claude 訂閱分開計費）、存 settings.aiApiKey 機密欄。
- **★4 配方要不要全站共用＋信任模型**：✅ **已拍板（William 2026-08-11）＝同意共用，硬條件＝配方只含版面規則、「零交易內容」必須有機械驗證**（不是口頭保證）。子項全數拍板：**格式定義 ✅（William 2026-08-14）＝格式 A「填格子」**（槽位只有字面文字＋枚舉、AI 只能填不能發明格子；外加比「regex 資源上限」更嚴的**配方裡零正則**——沒有 regex 就沒有 regex 炸彈；骨架沿用 storeRules 驗證過的「純文字＋跳脫」路線）；**畢業門檻 ✅（William 2026-08-14）＝5 次**（連續 5 份同版面、配方獨自解析、全過強閘＝穩定；中途閘紅＝該份退回 AI、配方標疑似過期、重生後從零重數）；**內建化 ✅（William 2026-08-14）＝兩訊號任一觸發進「內建化候選清單」、由人決定**（①用的人數超過 25 位②配方累計重生 5 次；不是自動轉寫——內建化買的是保護等級不是速度）；**版本與回滾 ✅（William 2026-08-15 拍板、同日翻案、同日第二輪補細部）＝留 1 版（現行＋上一版）、回滾自動**；順序＝閘紅先自動退上一版重解（免費）、上一版也紅才送 AI 重生（三方設計諮詢（Grok＋Codex 同建議）後 William 原話「配方版本改可滾回」）；**發佈模型 ✅（William 2026-08-11 拍板佇列；2026-08-15 補裁）＝審核佇列留到 P3 多人時再做**（P2 配方只存自己資料庫、「推全站」情境不存在；配方記錄預留狀態/版本/統計欄位）。
- **★5 時程**：學習模式中，本計畫先躺著；你喊「開工 P0」才動。✅ **2026-08-11 William 喊「開工 P0」＝P0 施工中**。
- **★6 弱閘／對帳失敗的匯入政策（r1 補）**：✅ **已拍板（William 2026-08-11）**＝①**對帳失敗（與帳單印的數字不一致）＝完全禁止匯入**（無覆寫按鈕；白話訊息帶數字與「回報」下一步）——P0 已落地於現有模板解析器；②**AI 路線（P1）的弱閘帳單＝不准匯入**（連總額都沒得對的格式，AI 的翻譯不收——寧可不吃，不吃來路不明的）；③模板解析器的弱閘＝維持今日行為放行（既有格式不因新閘退步），僅在回應標示級別。弱閘因此對 AI 是真的閘、對模板是標籤——兩者刻意不同，勿混。

## 八、誠實劃界

- 對帳閘擋「不一致」，**擋不住「自洽的錯」**（防線見 §二-2，壓低不歸零）；「數學剛好對但說明欄抄錯字」型靠學習表與人工改，不是新風險。
- 掃描圖片型帳單需 OCR 前置，另案另估。
- 弱閘＝標籤＋人工，誠實標示、不裝強。
- AI 不是 100%。承諾是「**造成不一致的錯會被擋下來給你看**」。**P1b-3 已實測（2026-08-13）**：
  故障注入 **23** 型 AI 誤讀，逐型量對帳閘攔不攔得住（考題＝`test/ai-gate-interception.test.js`，
  數字由該考題釘住、與本節互扣，改一邊另一邊就紅）：
  - **A 類｜會讓匯入的帳本內容出錯（金額／方向／帳戶歸屬／筆數／去重鍵），而且造成前後不一致 ＝ 9/9 全數攔下**（金額抄錯／金額差一個數量級／方向看反／
    漏讀一整筆／同筆讀成兩筆／餘額抄錯／掛錯帳戶／期末對不上概要／整份只讀出一筆）。
    **這就是上面那句承諾的全部內容**——它承諾的是「不一致會被擋」，不是「AI 不會讀錯」。
  - **B 類｜會讓帳本內容出錯、但閘看不到 ＝ 12 型全數漏接**（已知盲點，逐條寫在預覽窗的收合區給使用者看）：
    ①每個帳戶的**第一筆**（驗算是拿它的餘額去比下一筆，它的**金額與方向**都沒被驗到——方向讀反＝收入被記成支出、**月收支同時失真**）②外幣明細 ③本期無往來的帳戶
    ④金額與餘額**一起**被改成自洽的另一組數字 ⑤某筆金額錯**且同一筆餘額空白**（那一對被跳過，但該帳戶
    還有別對在驗 ⇒ 逐帳戶覆蓋不會紅）⑥一筆支出與一筆收入被**併成一筆淨額**（餘額鏈完全接得上，但收入與
    支出總額都錯——最陰險的一型）⑦**整個帳戶被漏讀** ⑧台幣帳戶被**誤判成外幣**（閘跳過、匯入層也排除
    ⇒ 該帳戶的交易一筆都不進帳）⑨**摘要或日期讀錯**——金額是對的，但那兩欄是**去重鍵的一部分**，
    下次重匯同一份帳單時會被當成新的一筆**再記一次**（端到端考題釘住：同一筆被記成兩筆；日期那型
    連「疑似重複」提醒都不會出現，比摘要那型更難發現）。
    ⚠️ **畫面上的編號是 ①～⑨、本節的型數是 12**：那不是筆誤——B8／B9／B10 是 ①與⑧ 的**另一種形狀**
    （整筆漏掉第一筆／外幣被認成台幣／首筆方向讀反），B11／B12（摘要／日期）共用 ⑨，寫成新編號只會讓畫面愈來愈長，所以把 ①⑧ 的說法擴寫涵蓋它們。
    ⚠️ **這份清單不保證完整**：⑤～⑧與 B8／B9／B10 那幾種形狀，都是本支送審後由複審者陸續找出來的——第一版曾寫「看不到的**四件事**」，
    那句話是**假的**。列舉補不完，所以畫面與本節都**不再宣稱窮盡**，改成明說「還有沒列到的」。
  - **D 類｜金額全對、但驗算蓋不到那個帳戶 ＝ 1 型**（某台幣帳戶餘額欄全空）：★6 逐帳戶覆蓋**保守拒收**。
    ⚠️ 它**不算在 A 類**——擋它的是覆蓋政策、不是偵測到不一致（`problems` 是空的）；混進 A 類會把攔截率撐高。
    ⚠️ **但別把它讀成「帳本不會出錯」**（r13）：餘額欄也是去重鍵的一部分，清空餘額＝指紋變了 ⇒
    先匯錯版再匯正確版會**重複入帳**（端到端考題釘住）。正式路線會被 ★6 擋下，所以不是生產漏洞。
  - **C 類｜不動到帳本金額 ＝ 1 型**（外幣明細金額讀錯——外幣本來就不匯入、錯的是畫面上的數字不是帳）：
    閘本來就不管。⚠️ **摘要與日期已於 r12 移出 C 類**：它們是**去重鍵的一部分**，讀錯會讓重匯時重複入帳
    ——原本歸在這裡，與本節對 A 類「去重鍵變了也算帳本出錯」的判準自相矛盾（複審指正）。
- **全管線重測（P1b-3 續集，2026-08-16；考題＝`test/ai-pipeline-interception.test.js`，數字與本段互扣）**：
  同 23 型注入改打正式 AI 路線的完整防線（驗收→**接地檢查**→**合計交叉驗證**＋閘；P2-2a 兩道上線後的現況）——
  **19 型擋下、4 型漏接（B7 台幣認成外幣／B9 外幣認成台幣／B11 摘要錯／B12 日期錯）**。
  量的情境＝**「兩讀都錯成同一份答案」的最壞情境（相關錯誤）**：P2-4 雙讀後，「一讀錯一讀對」的不相關錯誤在**比對欄位**上被比對器看到——摘要／備註／日期都在比對清單（撐這句的考題＝`test/ai-dual-read.test.js` 的比對器逐欄承重題，不是本卷），所以 B11/B12 只剩「兩個模型錯得一模一樣」這條路。
  主要收獲＝閘單獨看不到的 12 型中 8 型現在有防線接住；C0 外幣金額錯也從「閘不管」變「接地擋」。**逐型歸因**（與考卷 defense 欄一字對應）：接地擋＝B1（真自洽組——鏈與合計全自洽、唯一破綻是湊的數字不在版面）/B3/B4/B5；合計擋＝B2（首筆金額、錯值刻意用版面他處印過的數字孤立這道）/B6/B8/B10。
  ⚠️ 有條件的擋、且**每句但書都有自己的負例考題撐**（N 系，逐項落點——刻意不寫死題數）：①整漏/方向＝N1/N2（拿掉合計列同刀重打＝missed）；④自洽撞版面＝N3（數字全印在利率雜訊列＋無合計＝三道全盲）；⑥兩條件同不成立＝N4；⑤餘額空白同刀＝N5；⑦整戶漏讀無合計＝N6——預覽窗盲點清單的但書逐條對應（2026-08-16 改版、逐字釘在考題）。
  ⚠️ 本段不取代上面「閘」的數字：閘的 A 類 9/9 是對使用者的承諾（不可漂）；這段是管線現況（防線增減要跟著重量）。
- ⚠️ **上面量的是「閘」的性質，不是「AI」的性質**：得到的是**條件攔截率**（「**如果** AI 犯了 X 型錯，
  會不會被擋下」），**不是**「AI 多常犯錯」。後者要真 AI ＋ 有標準答案的語料才量得出來，**尚未做**——
  在那之前**不得**對外宣稱任何「AI 正確率」。不打真 AI 量攔截率的三個理由：①不可重現（同一份帳單兩次
  可能給出不同答案，票制存在的理由就是這個）②要花使用者自己的錢 ③跑得動的份數小到沒有統計意義。
- AI 供應商側的留存與訓練政策＝★1/★3 的裁決條件，不是本計畫能單方保證的事。

## 九、考題策略

- **合成語料庫**：數家假想銀行版式（直式/橫式/跨頁/千分位/民國年/幣別欄差異），絕不用真帳單；帳號末碼一律明顯假值。
- 突變**按閘分級宣稱**：強閘（改壞一筆金額/方向/偷刪一筆→紅）；中閘（改壞合計/漏調整列→紅）；弱閘（只宣稱結構壞→紅）。
- 配方回退行為題：配方壞→閘紅→依同意粒度走回退。
- 機密流向考題：密碼與內文不落 log/快取。
- 真帳單只做本機煙霧測（既有慣例）。

## 十、名詞小辭典（白話）

- **API**：軟體之間的「服務窗口」——app 把資料寄過去、對方把結果寄回來。
- **API key（鑰匙）**：使用服務窗口的會員憑證，一串長密碼；窗口憑它認人、記帳。絕不能放進網頁前端，只能放伺服器。
- **token**：AI 計費的字數單位，一個中文字約 1–2 個 token；「每百萬 token 幾美元」就是 AI 的度量衡。
- **OCR**：把「圖片上的字」辨識成「文字」的技術——掃描型帳單（照片、影本）要先過這關才有文字可解析。
- **宣告式規則**：只描述「什麼位置是什麼欄位」的資料，像填空表格；相對於「可執行程式」，它不能做任何動作——這排除了「執行任意程式」那一類風險，**較適合受控共用；但可不可以共用，仍要通過 ★4 的零內容機械驗證與信任模型**（規則檔本身仍可能夾帶內容、壞規則或耗資源的樣式）。

## 變更紀錄

- 2026-08-10 建立（William 指示把三件組寫成白話計畫）。
- 2026-08-11 r1 四高一中全修：§三改為機密流向誠實現況（密碼儲存兩制／兩道邊界）；對帳閘承諾改「擋不一致、不擋自洽錯」＋防線；接縫改列真實施工範圍（銀行身分維度／配方儲存）；補 ★6 與 ★1/★3/★4 子項；成本改附計算基礎＋待實測；補名詞小辭典。**★2 拍板＝銀行先（William 2026-08-11）**。
- 2026-08-11 r2 一高兩中全修：閘承諾再收斂（「造成不一致時能擋」；控制總額非獨立解析器；攔截率待實測不先承諾）；匯出邊界逐字對齊契約（LOCAL 匯出完整含密碼、HOSTED 才剝除）；辭典「宣告式＝可安全共用」改「較適合受控共用、仍要過 ★4」。**三項拍板寫入（William 2026-08-11）：★1＝(c) 折衷；★4＝同意共用（硬條件＝零內容機械驗證）；P0.5 匯入密碼流程統一（銀行密碼改可選儲存、比照卡密待遇）**。P1 加 HOSTED 同意機制停止線。
- 2026-08-11 P0 開工（本支）：**★6 拍板＝對帳失敗禁止匯入（無覆寫）＋AI 路線弱閘不准匯入（模板弱閘照舊放行）；★4 子項拍板＝新配方先進審核佇列**。落地＝對帳閘模組 `lib/statement-reconcile.js`（強＝銀行餘額鏈＋末筆對概要／中＝卡帳等式＋明細對總額／缺數字誠實降級）＋中閘四格抽取 `extractStatementTotals`（與應繳金額共用序數對位機關），接線於銀行預覽/套用與卡片兩預覽入口；收支契約新增「匯入對帳閘」節。前端就地顯示等前端支另接。
- 2026-08-11 P0 r1 三高兩低全修：①中閘「明細加總 vs 摘要總額」改**影子檢查不擋**（合法摘要調整型會被誤擋——r1 用反例坐實；摘要等式仍擋下型）②強閘末筆對概要只認**真正末筆**（末筆餘額 null＝skip，不再拿較早餘額冒充）③強閘首筆未驗＝**誠實揭露**（§四表格改口「期初＋Σ＝期末未實作」、verdict 計數、考題釘界線）④四格帶到預覽回應⑤本節開頭過期總括與 §六/§八 節次引用修正。
- 2026-08-11 P0 r2 一中四低全修：①補「只剩影子 mismatch＝level 必為 weak」考題（r2 實測把 level 改成任何非 skip 都算 medium 原本全綠）②previewAuto 四格逐欄斷言③assertCardReconciled 註解跟上影子語意④「官網 XLSX 四格全 null」更正為「只有本期帳單金額＝due 讀得到、缺三個交叉欄」（契約與 fixture 同步照實）⑤檔頭總括改成「每期動工前該期裁決點先拍板；未決子項只擋所屬 P1/P2」。
- 2026-08-11 **P0.1（#439 合併當天、William 真帳單煙霧測抓到）**：強閘誤擋外幣綜合帳戶——同一個遮罩帳號掛多幣別（概要同帳號多列、明細各幣別共用帳號欄且列上無幣別），按帳號驗鏈必把兩條幣別攪在一起。修＝**強閘只驗台幣帳戶（閘的射程對齊匯入的射程：外幣明細本來就不入帳）**，外幣整組 skip 並誠實計數；查無幣別＝比照台幣驗（匯入 fallback 同向）。煙霧測同時留下觀察：外幣明細列的金額有讀成 0 的跡象（外幣利息列金額讀為 0、餘額卻有微幅增加；帳號與數字不留存——真帳單資料＝PII 不進版控）——不入帳故不修，未來要匯外幣現金流時先回頭校這裡。P0.1 r1 三條全修：①考題與文件的真帳單末碼/金額/描述全換合成假值（PII 鐵則）②幣別判準抽成單一份（`statementCurrencyLookup`＋db 補位＝與匯入端 `txCurrency` 同一條鏈、跨層考題釘四種來源）③容差 0.005 歸因更正（IEEE 浮點殘差，非外幣兩位小數）。
- 2026-08-11 **真帳單校準第一批（核對五份帳單全數 100% 對上 app 資料）**：①強閘在兩份真銀行對帳單全綠（餘額鏈 29＋45 對、末筆對概要 4＋4 全過＝「現值參考日＝明細期末」的版面假設**成立**）②中閘在真郵寄版信用卡 PDF 首度全開：**等式四格全中、C1 pass**＝序數對位在真版面成立；**兩道影子檢查的差額同值、且恰等於該期退款總額**＝版面語意實測「本期新增款項＝正項−退款（淨額）、已繳款桶不含退款」——與 C2/C3 目前假設（退款在已繳桶）相反。**升級影子為擋下時採：C2′＝明細正項−退款絕對值 vs 新增款項、C3′＝繳款列（isCardPayment）絕對值 vs 已繳款桶**；一份樣本不足以定案，再收 2–3 份郵寄版驗證後另支 PR 升級（金額不留存——真帳單資料不進版控，只記結構結論）。③官網 XLSX 如預期弱閘（只有 due）。④帳單印的卡號末四碼與使用者現役卡不同（補發換卡）＝催生候選 6-16「舊卡末四碼」。
- 2026-08-11 **P0.2（影子公式校準修正）**：又驗三份郵寄版（合計四份、退款筆數各異）＝修正公式**全部精準吻合**→落地：②改「明細正項−退款 vs 新增款項」③改「繳款列（isPayment）vs 已繳款桶」（負數缺旗標＝用 desc 過 isCardPayment 重判——單一真相共用；連 desc 都沒有＝兩道影子誠實 skip、不猜）；**維持影子不擋（William 裁示：先修公式、暫不升級）＋⏰ 提醒絆線寫進契約與模組**（分期/年費/循環息月份仍安靜＝提請裁決升級為擋——兩處 advisories.push 改 problems＋考題同步、一起動）。stats 增 refundAbs/payAbs/unjudgeableNeg 供前端顯示與後續校準。
- 2026-08-12 **P1b-2 前端落地（本支）**：AI 同意確認窗（每次都問、不記住；顯示送哪份/去向/費用級距）＋預覽窗誠實徽章＋設定頁鑰匙欄（不回顯、四顆就地解釋）。後端配套＝`lib/bank-statement.js` 兩處 throw 加 `code:'bank_unrecognized'`（AI 入口的唯一判準；**對帳閘紅刻意不給 code**＝★6 禁止匯入的畫面不會長出 AI 入口）。判準與文案抽成純函式模組（`ai-consent.js`／`ai-key-settings.js`）才有行為考題；接線層只能去註解形狀掃描（`cashflow.js`／`settings.js` 頂層 import `app.js`、node 載不動整頁）＝家規核可劃界寫在考題檔頭。另補 `settings.aiApiKey` 的 HOSTED 端到端五題（P1b-1 只有單元級）。
- 2026-08-12 **P1b-1 開工（William「動工 P1b-1」）＝★3 拍板寫入＋後端引擎落地（本支）**：雙檔分工（ai-parse 純模組／ai-transport 唯一外連檔＋真引擎工廠——外連能力不進 crud.js 的 import 閉包、入外連登記閘與 OUTBOUND_ENDPOINTS）；四道規矩（HOSTED 停止線在鑰匙檢查前／useAi AI 要求旗標（時稱「同意旗標」，該舊名已於 #455 棄用）＝確認窗蓋好前後端不通／aiApiKey 機密五件套／★6 AI 只收強閘）；模型階梯 Haiku→Sonnet 一次；引擎交原始答案、服務層自驗收（縱深防禦）；apply 讀→閘→套→寫不變量原樣（AI 的外部 IO 全在寫入用 getDb 前＋寫入前 fresh db 重過閘）。收支契約新增「AI 解析路線 P1b」節；考題＝test/ai-parse.test.js（假引擎、P1a 互扣）。
- 2026-08-12 **P1 開工（William 喊「解析器下一期（P1）開工」）＝三項拍板＋P1a 落地（本支）**：拍板①P1 拆 P1a（銀行身分維度，先做、不需鑰匙）／P1b（AI 引擎，待 ★3）；②★3 先做供應商比較功課再裁（必查欄含留存/訓練政策；Anthropic 基準點已查——商業 API 不用於訓練、30 天內刪除）；③★1 子項 LOCAL 同意粒度＝**每次送 AI 前先跳確認窗**（顯示送哪份、去向、估計費用；不記住同意）。P1a 落地＝`matchAccount` 機構規則（帳戶 `bank` 欄：新建蓋戳＝帳單宣告、比對成功不回填＝推論不入檔、無戳＝祖父條款照舊只比數字＋誠實劃界理論碰撞）＋去重鍵雙格式（台新 `bank|…` **位元組級凍結**；他行 `bank2|機構|…`；stage 3 初版末碼舊鍵只在台新查）＋三個 bankRef 消費者雙軌解析（方向／自動名／改名對齊）＋自動名與 812- 顯示補碼帶機構（台新輸出不變）。考題與突變的守門範圍＝`test/bank-statement.test.js`「機構維度」節（顆數與刀數刻意不寫死——r1/r2 各補過一批，寫死的數字自己會漂）。
- 2026-08-15 **P2 開工（William「開工 P2 配方快取」）＝★4 子項全拍板寫入＋P2-1 配方純模組落地（本支）**：格式 A（零正則）／畢業 5 次／內建化候選兩訊號／版本可回滾（同日翻案，細部 P2-2 裁）／佇列留 P3；`lib/parse-recipe.js`（驗證器＋泛化引擎＋出生驗收）＋考題（兩套虛構版面＋台新等價交叉驗證）；收支契約新增「配方快取 P2」節。
- 2026-08-15 **P2-2a AI 路線強化（本支）＝裁示④細部⑥⑦⑧寫回＋落地⑥⑧**：模型 Sonnet/Opus、接地檢查、合計欄交叉驗證；⑦（雙讀）與「寫配方用 Opus」落點在 P2-3。
- 2026-08-15 **P2-2 儲存與讀取接線落地（本支）**：先配方後 AI 的讀取順序、版本自動回滾、畢業與疑似過期計數全上線；細節見 §五 P2-2 與收支契約。
- 2026-08-16 **P2-3 AI 產配方落地（本支）＝P2 配方快取三支全數收官**：生成掛 apply 成功後、一律 Opus、出生三關、重生與 rebirths 接線、A6「份」判準明文、前端徽章；裁示⑦雙讀整合流程草案入 §五（當時待 William 過目；後於 2026-08-16 拍板定稿＝見下一筆）。
- 2026-08-16 **裁示⑦定稿寫回（#472）**：William 拍板兩點＝a) 雙讀不一致送 Fable（claude-fable-5）第三讀仲裁（獨立重解；與恰一份全欄一致＝採用、三份互不相同＝標紅＋手動記帳；無「使用者自挑」門）b) 開關預設開；升級階梯取代關係維持草案項、落地走獨立支（成本句連動記待辦）。
- 2026-08-16 **P1b-3 續集＝全管線攔截率重測（本支）**：23 型注入改打驗收→接地→合計＋閘全管線＝19 擋 4 漏（相關錯誤情境；不相關另有雙讀比對器——錨定在 ai-dual-read 逐欄承重題）；§八新小節與考卷**機械**互扣（考卷讀計畫檔驗數字）；N 系條件負例讓每句「仍看不到」都有自己的題撐（含 Codex r1#3 補的 B4/B6 同刀）；預覽窗盲點清單逐條補但書（文案 Claude 起草、待 William 審）。Grok 預審 r0 三高全修＝B1 改真自洽組、B2 孤立合計道、A4 與 B8 分刀（漏中間 vs 漏首筆）。
- 2026-08-17 **P2-4c 帳號正規化（本支）**：William 核准「剝分隔符＋統一用完整號碼認身分」——canonMasked（程式端、AI 照抄鐵則不動）＋maskedCmp 三態（same/conflict/variance）：分隔符差異歸零、前綴都印得出且對不上＝不同戶＝hard（幣別表「帳戶帳號」/帳戶層/逐筆三處）、少印前綴＝建議面；碰撞鍵改用正規形。
- 2026-08-17 **P2-4b 比對器真帳單校準（本支）**：William 上傳第一份真帳單就被 ai_disagree 擋（分歧全在機構名/備註的寫法差異、錢欄位全同）＋仲裁等五分鐘——裁示「移出機構名＋備註」＝比對改兩層（hard 觸發／textVariance 建議面：機構名・備註・帳號印法→不觸發、徽章 ✏️ 句顯示**實際中選模型**——一致路採 Opus、仲裁路可能採 Sonnet）；帳戶錢組成改末碼身分比對（印法差異降建議面）；busy 鈕字改誠實（雙讀時間預期）。大多數上傳不再仲裁＝時間回到並行單讀級。
- 2026-08-16 **P2-4 雙讀＋三讀仲裁落地（本支）**：aiBankRoute 雙讀編排（Sonnet＋Opus 並行→aiAnswersAgree 錢欄位比對→一致採 Opus 版；不一致 Fable 獨立仲裁→恰一份全欄一致＝採用；三讀不同＝ai_disagree 列欄位不列值）；settings.aiDualRead 預設開（dualReadWanted＝只有明確 false 才關）；關閉＝回單讀＋升級階梯（原路一字不改）；徽章「雙讀一致／三讀仲裁」＋設定頁開關＋就地解釋；成本句四處改「至多 4 發」。
